# 实验流程（EXPERIMENT.md）

> 排版引擎的每次改动都按这个循环走。它不是形式主义：这套流程在 2026-07-20 一天里
> 抓出了弃页缺陷、9% 测量虚高、搜索兜底返最差版本三个真 bug——每一个都是靠流程里的
> 某一步暴露的，跳过哪步就漏哪类。
>
> 材料（fixture）的入库边界与学科规则的维护纪律见 [RULES.md](./RULES.md) §2.4，本文只管"怎么做实验"。

## 五步循环

```
① 现象定位 → ② 离线验证方向 → ③ 单变量 A/B → ④ 目检 → ⑤ 固化
     ↑              （毫秒级）        （秒级）      （眼睛）      ↓
     └────────────── 出现意外回归：做归因对照（见下） ←──────────┘
```

### ① 现象定位——先有判例再动手

每个实验从**一个具体判例的具体现象**出发（"block-14 卡住、第 2 页空 2/3"），没有能复现的现象不开工。

```bash
cd packages/server
npx tsx test/dump-features.ts              # 全部判例的特征 + 规则判定一屏速查
npx tsx test/dump-features.ts poli-econ.md # 只看一个
```
网页端勾「显示网格」（或 `/api/scene` 传 `debug: true`）能把块边界/留白/档位直接画在 PDF 上。

### ② 离线验证方向——便宜的先跑

改算法前先在纯算法层证明方向有收益、且不变量不破（不开浏览器，毫秒级）：

```bash
pnpm test    # 拼装不变量随机压测 / 规则引擎判例对账 / 学科层行为锁 / AI 遮罩安全网
```
新机制先写成 scratchpad 脚本做随机压测（如 500 组统计"repack 省 3.7%、backfill 省 22%"），
证明值得做再动真格。这一步把大部分错误挡在浏览器实验之前。

### ③ 单变量 A/B——一次只切一个开关

```bash
npx tsx test/run-ab.ts poli-econ.md 2        # 三种拼装变体：字号/页数/逐页填充率对照
npx tsx test/run-ab.ts os-large.md 1 text-cram
```
关键纪律：**老行为必须随时可复现**（开关做进参数，而不是删掉旧代码）——没有对照组，
一切"提升"都是自说自话。更细粒度的单跑用 `test/run-grid.ts`（见其头部注释）。

### ④ 目检——数字看不出的问题靠眼睛

```bash
swift test/tools/pdf2png.swift test/fixtures/poli-econ.grid.pdf /tmp/poli && open /tmp/poli-p*.png
```
弃页、标题沉底、诡异空隙都是这步抓的——字号和页数指标里完全体现不出来。**每轮实验至少目检一次**。

### ⑤ 固化——结论进测试和规范，不留在聊天记录里

- 确定性行为 → `test/unit/*.test.ts` 加断言（修过的 bug 必须有回归锁）
- 数值与取舍 → RULES.md：判例表（§1.6）、证据等级（§一）、缺陷台账（§4）
- 判据：下次改引擎，`pnpm test` 一条命令就该知道有没有踩坏今天的结论

## 两个专项打法

**归因对照**（改动后出现意外回归时）：变化里若混了两个因素（如"钉住页首块"+"best-fit"），
临时关掉其中一个重跑，锁定真正的原因，然后才谈取舍。**一次只检验一个变量。**

**先定量再修**（怀疑某处有问题时）：不直接改，先写诊断脚本把问题量化成数字
（"首边距 62mm + 尾空白 16mm = 虚占 9%"）——有数字才知道值不值得修、修完该省多少；
修完**复跑同一脚本**对账。诊断脚本可以临时，结论必须落 RULES.md。

## 真材料实验的固定动线

拿到一份新材料（入库边界见 RULES.md §2.4）：

1. 脱敏 → 存 `test/fixtures/`，顶部注释写来源与授权（真实 / 合成 ✳️）
2. `dump-features.ts` 看特征与判定——判错了就是新发现
3. `run-ab.ts` 跑排版 + 目检 PDF——观感翻车也是新发现
4. 每个发现过一遍守门问题（RULES.md §2.4 ③）分流到力学层或学科层
5. 校准结论写进 RULES.md（阈值 ◐→● 的唯一途径），行为锁进 `test/unit/`

> 先例：poli-econ（2026-07-20）一份材料校准了公式密度线（与 test.md 两侧夹逼）、
> 实锤了阈值悬崖、当场暴露并修掉两个 bug——按这条动线走完就是这个产出。

## 十分钟北极星演练（发布前手动验收）

产品北极星：**应急路径——从"只有材料"到可打印 PDF ≤ 10 分钟**（定位见 README
「产品定位与增长策略」）。每次发布/大改前用一份**没喂过引擎的真实无结构材料**
（Word/课件粘贴形态）实测一遍，逐段计时：

| 计时段 | 起止 | 目标 |
|---|---|---|
| T1 可达 | 打开网址 →（口令）→ 页面可用 | ≤ 1 min |
| T2 材料就绪 | 粘贴进对话窗 → 追加完毕 | ≤ 2 min |
| T3 标准化 | AI 开始转换 → 标准 .md 通过结构体检 | ≤ 3 min |
| T4 排版 | 点「采用并排版」→ PDF 预览出现 | ≤ 2 min |
| T5 到手 | 下载 → 确认可读（抽查公式/表格） | ≤ 2 min |

记录规则：总时长 >10 min 时,最长的一段就是下一个要修的瓶颈,把数字记在本节下方的
演练日志里(日期 · 材料形态 · 各段耗时 · 瓶颈与处置),不新开文档。

### 演练日志

（暂无——待 ⓪ 对话入口上线后跑第一轮）
