# 分工与协作规则(所有人开工前必读)

> 这份文件是全项目唯一的分工总表。
> **规矩只有两条:开工前从头读完本文件;完工后回到 §4 任务板「划一笔」。**
> 没划账的工作视为没完成,别人可能重做一遍。
> 本文件由主程序负责人维护,想调整分工先找负责人打招呼,不要直接改。

---

## 1. 角色

| 角色 | 是谁 | 管什么 |
|---|---|---|
| **主程序负责人** | fu10085cong-ops(仓库所有者) | 核心引擎、判据阈值、权威文档;验收并合并所有人的工作 |
| **协作者** | 认领任务的其他人 | 从 §4 任务板认领「绿区」任务,一次只认领一件 |

---

## 2. 红区与绿区(先分清自己能碰什么)

### 红区 —— 只有主程序负责人能改,其他人只读

| 范围 | 为什么不能碰 |
|---|---|
| `packages/server/src/engine/` 全部 | 排版/转换/检索核心。判据和阈值互相牵连,改一个值要跑全套 A/B 实验 |
| `RULES.md` / `TESTING.md` / `FORMAT.md` / `EXPERIMENT.md` | 权威文档,有单测契约锁和代码对着念,改文档不改锁会直接红 |
| `packages/server/test/` 下所有测试与基线 | 锁就是规则本身;基线数字是实验入账的证据 |
| `.env` 及一切密钥 | **永不入库、永不截图、永不粘贴给任何人**(包括 AI 对话) |
| `Dockerfile` / `docker-compose.yml` | 部署关键,改动必须双平台实测验收 |

**在红区发现 bug 或想改进:不要动手修,写到 §5 问题登记表,负责人来处理。**

### 绿区 —— 欢迎认领

- `packages/web/` 的样式(`Studio.css`)、界面文案、独立小组件
- `DEPLOY.md` 等面向使用者的文档校对与步骤实测
- 收集真实测试材料(讲义、试卷的 Word/PDF)
- 跑腿验证类:按清单实际操作一遍,记录结果

拿不准算红区还是绿区 → **先问负责人,再动手。**

---

## 3. 干活的规矩(所有人一样)

1. **开工前**:读完本文件;认领任务时把 §4 对应行的状态改成 `🔨`,写上自己名字。
   一次只认领一件,做完再领下一件。
2. **开分支干活**,不直接改 main:
   ```bash
   git checkout -b 任务编号-简述   # 例:t1-copy-button
   ```
3. **完工标准以任务板那一行写的为准**,不是"我觉得差不多了"。
   最低线人人一样:改了前端必须 `pnpm --filter @halfhalf/web lint` 绿;
   动了任何 `.ts` 必须 `pnpm test` 绿。**改过不等于跑过——没跑过的验证不算验证。**
4. **完工划一笔**(必须做,否则视为未完成):
   - 回到 §4 任务板,把状态 `🔨` 改成 `✅`;
   - 填上完工日期和一行证据(跑了什么命令、什么结果,例:`lint 绿,截图目检过`);
   - 推分支,通知负责人验收。**合并权在负责人,划账 ≠ 已合并。**
5. 中途放弃或搁置:状态改回 `☐`,名字划掉,在备注里写一句为什么——
   让下一个认领的人不掉进同一个坑。

---

## 4. 任务板

状态:`☐` 待认领 · `🔨` 进行中 · `✅` 已完成待验收 · `✔️` 已验收合并

### 主程序(负责人专属,他人勿领)

| 状态 | 编号 | 任务 | 完成标准 |
|---|---|---|---|
| 🔨 | M1 | 核心引擎持续优化(排版/转换/检索判据) | 按 EXPERIMENT.md 五步循环,结论入 RULES.md |
| ☐ | M2 | 真材料端到端验证(自己的 Word 讲义排成 PDF) | 目检排版正确,问题记入缺陷台账 |
| ☐ | M3 | 各挂账实验(镜像瘦身、repack 降级保序、标题跨栏等) | 单变量 A/B + 目检,零挂账收官 |

### 细枝末节(欢迎认领)

| 状态 | 编号 | 任务 | 干什么 | 涉及文件 | 完成标准 | 认领人 | 完工日期 / 证据 |
|---|---|---|---|---|---|---|---|
| ☐ | T1 | 对话回复「复制」按钮 | 在 AI 回复卡完成态动作区加一个复制按钮,点击把回复文本写入剪贴板 | `packages/web/src/components/Studio/MessageCard.tsx` | lint 绿;浏览器实测点击后能粘贴出全文 | | |
| ☐ | T2 | 左栏卡片悬停高亮 | `.hh-source-card:hover` 悬停时边框变主题蓝(用 token,不写死色值) | `packages/web/src/components/Studio/Studio.css` | 目检生效;全文件无新增写死色值 | | |
| ☐ | T3 | 界面文案通读校对 | 通读三栏所有提示文案,揪错别字、病句、含义不清的说法,列成清单 | `packages/web/src/components/Studio/*.tsx` | 清单交负责人;经确认的改动 lint 绿 | | |
| ☐ | T4 | DEPLOY.md 步骤实测 | 找一台干净机器,严格按文档从零部署一遍,每一步记录卡不卡 | `DEPLOY.md`(只记录,发现文档错误报负责人) | 完整走通或列出卡住的步骤清单 | | |
| ☐ | T5 | 收集真实测试材料 | 收集 5 份以上真实讲义/试卷(Word/PDF),注明来源与授权情况 | 交给负责人,**不要自己放进仓库**(版权与隐私要负责人过目) | 材料交付且来源清楚 | | |
| ☐ | T6 | 拆分 panels.tsx | 761 行的文件按面板拆成多个文件,**纯搬家不改任何行为** | `packages/web/src/components/panels.tsx` | lint + `pnpm exec tsc --noEmit` 绿;界面目检无任何变化 | | |

> 任务板会随项目推进增删行。增删由负责人操作;想加任务,先在 §5 登记。

---

## 5. 问题登记表(发现问题写这里,不要顺手修)

| 日期 | 发现人 | 问题描述(现象 + 复现步骤) | 涉及区域 | 负责人处理结果 |
|---|---|---|---|---|
| | | | | |

---

## 6. 三条铁纪律(违反任何一条,当次工作作废重来)

1. **`.env` 和密钥永不入库。** 提交前自查:`git status` 里绝不能出现 `.env`。
2. **红区只读。** 引擎、权威文档、测试基线,发现问题走 §5 登记。
3. **没跑过验证不划账。** 划账那一行必须写得出"跑了什么、结果是什么"。
