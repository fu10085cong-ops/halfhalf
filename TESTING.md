# 测试标准（TESTING.md）

> 每个**产出环节**（输入 → 产出物）都按这套标准立测试。它回答两个问题：
> "产出坏了没有"（能自动判定）和"产出变好还是变坏了"（要人裁决）。
>
> 与 [EXPERIMENT.md](./EXPERIMENT.md) 的分工：那份管"怎么做引擎实验"，这份管"每个环节
> 该有哪些测试、判据是什么、跑不跑门禁"。排版环节两份都适用。

## 0. 为什么分三层

**假 AI 单测永远测不到提示词缺陷。**

这不是推论，是判例：精简环节的 6 个单测长期全绿，而真实产出**恒定失败**——提示词写着
「章节标题…不要输出」，而遮罩后标题恰恰是正文里那个必须原样保留的占位符，模型照做删掉它，
安全网逐块判"占位符丢失"，带标题的块（几乎是全部）一个都精简不了。单测注入的假 AI 总是
守规矩地保全占位符，测的是**闸的行为**，压根碰不到**提示词与遮罩打架**这一层。
这个缺陷是 2026-07-29 立本标准、第一次跑 `pnpm eval` 当场抓出来的。

所以三层各司其职，缺一层就漏一类：

| 层 | 测什么 | 抓得到 | 抓不到 |
|---|---|---|---|
| **L1 产出闸** | 产出满不满足硬契约 | 单次产出的结构/保真缺陷 | 闸本身写错 |
| **L2 行为锁** | 闸在各种输入下的行为 | 闸的逻辑缺陷、误报漏报 | 提示词缺陷、模型退化 |
| **L3 质量评测** | 真 AI 的产出质量 | 提示词缺陷、换模型的影响 | 罕见输入的边角 |

---

## 1. 三层定义

### L1 产出闸（Gate）— 代码里自带，恒在

每个环节必须有一个**纯离线、毫秒级**的"产出必须满足 X"的校验，红线级判据。

- 不合格时的行为只有两种：**明确失败**（报错码）或**安全降级**（保留原文/标注不合格并交由用户判断）。
  **绝不允许静默放行**——"看起来正常的坏产出"会被用户打印出来带进考场。
- 闸必须是纯函数或近似纯函数，不联网、不调模型：它要在每次真实请求里跑，也要能被 L2 反复调用。

### L2 闸的行为锁（Lock）— `pnpm test`，硬门禁

- **假 AI 注入**：调模型的环节必须提供注入点（`streamFn` / `deps.chat` / stub provider），
  让全流程能在零 token、不联网的条件下跑通。
- **双向断言**：每条判据既要测"该拒的拒"，也要测"不该误报的不误报"。
  只测前者会养出一个把什么都拒掉的闸。
- **流程编排断言**：失败路径的调用次序也要锁（例："搜索返回 0 条时绝不调用总结模型"）。
- 判据：**必须全绿**。改任何代码，`pnpm test` 一条命令就该知道有没有踩坏既有结论。

### L3 产出质量评测（Eval）— `pnpm eval`，对账型

固定评测材料 × 真 AI × **确定性指标** × 基线对账。

- **不用 AI 评委**：评分器自身漂移会毁掉基线对账，且评委与被评同模型时有偏。
  只测能确定性度量的属性。
- **核心指标是锚点召回率**：每份材料在 `test/eval/manifest.json` 里显式标注
  **anchors**（必须保留的知识锚点：数字、术语、标识符）与 **noise**（应当剥除的噪声：
  页眉页脚、口水话）。产出做归一化子串匹配后统计命中率。纯正则，零 AI。
- 材料与锚点**冻结**：不许为了让数字好看临时改锚点。加材料 = 加一条 manifest + `--update`，
  在提交里说明。

**锚点怎么选**（决定评测有没有效）：

- 选**改写后仍会逐字出现**的东西：数字（`0.67`、`1840`）、中文术语（`机会成本`）、
  代码标识符（`binary_search`）。
- **不要选整句话**——它一定会被改写，只会制造假阴性。
- **结构化的锚点不能用公式**：它的任务本来就是把公式转成 LaTeX，文本形态必变。
- **精简的锚点可以用 LaTeX**：公式被遮罩成哨兵、必须逐字回填，形态不变。

---

## 2. 各环节的标准

| 环节 | L1 产出闸 | L2 行为锁 | L3 评测 |
|---|---|---|---|
| ⓪ AI 结构化 | `checkStructure` + `checkElementWhitelist`（4 条）+ `checkFabrication` | `ai-structurize.test.ts` | `pnpm eval -- structurize` |
| ① AI 精简 | 三道安全网：原子保全 / 公式预检（`precheckFormulas`）/ 确实精简 | `ai-compress.test.ts` + `atom-mask.test.ts` | `pnpm eval -- compress` |
| ② 联网补洞 | 4 段 throw + 质量闸 + 恒定 warning | `research-pipeline.test.ts` 等 3 份 | `pnpm bench:research`（观察型） |
| ③ AI 对话 | **无（有意）——闸在下游** | `ai-chat.test.ts` | 不单独评（见下） |
| ④ 文档导入 | 错误码 + 路由判据 + 空成功拒绝 | `document-import.test.ts` + `knowledge-ir.test.ts` | `pnpm bench:document`（门禁型） |
| ⑤ 重点规划 | `validateDocument` / `validateInputs` + fileHash | `restructure-*.test.ts` | 不需要（纯确定性算子） |
| ⑥ 排版 | `isAcceptableTrial` + 超高/cramped 显式上报 | 6 份，含 60 组随机压测不变量 | `pnpm bench`（对账型）+ `pnpm qc:render`（门禁型） |

**③ 对话为什么没有产出闸**：它的产物进生产链路时必过**统一输入闸**——「存为新材料」「写回」
都会跑一遍 structurize（见 [统一输入闸设计](./docs/superpowers/specs/2026-07-28-unified-input-gate-design.md)）。
闸在下游是设计，不是缺口。对话自身只有输入侧约束（历史截断、材料注入）和提示词红线，
两者都由 L2 锁住。**若将来对话产物出现绕过下游闸的新出口，必须同时补 L1。**

**⑤ 重点规划为什么不需要 L3**：`restructure-plan` / `materializer` 是纯确定性算子，
不调模型，L2 的断言就是完整规格。

---

## 3. 门禁强度：每个跑分器必须自报家门

三种强度不能靠读代码猜，**每个 bench/eval 脚本的头注释必须写明自己是哪一种**：

| 强度 | 含义 | exit code | 现有脚本 |
|---|---|---|---|
| **门禁型** | 绝对阈值，破线即失败 | 破线 → 1 | `pnpm test`、`pnpm bench:document`（7 条 gate）、`pnpm qc:render` |
| **对账型** | 与冻结基线逐项 diff，由人裁决 | 恒 0（除非崩溃） | `pnpm bench`、`pnpm eval` |
| **观察型** | 只打印供人看，无判据 | 恒 0 | `pnpm bench:research` |

**AI 环节为什么是对账型而不是门禁型**：模型输出有随机性，硬门禁会持续假阳性，
最后的结局一定是"大家都学会了忽略这个红灯"。对账型把判断权交给人，但基线让"变差了"
无处可藏。唯一的例外是红线指标（见下），它下降必须解释，不许当噪声放过。

### L3 的红线与黄线

| 等级 | 触发 | 处置 |
|---|---|---|
| 🔴 红线 | **锚点召回率下降** | 必须解释或修复。它意味着知识点丢了——保真红线被击穿 |
| 🔴 红线 | **结构化的闸由绿转黑** | 产出结构不合格 |
| 🟡 黄线 | 精简的安全网拦截、采纳块数下降、噪声剥除率变化 >5pp、轮次增加 | 记录趋势，连续两轮恶化才动手 |

**精简的安全网拦截为什么只是黄线**：拦截 = 保留原文 = 安全降级，产出没坏，只是没优化成。
弱模型上本就偶发（首轮基线里 `verbose-table` 的表格块就是这样），当红线会天天刷假警报。

**读精简的锚点召回率要配着采纳率看**：全部块被拦下时原文原样保留，召回率恒 100%——
那是退化的满分，不是好成绩。

---

## 4. 加东西的流程

**加评测材料**：材料放 `test/eval/<stage>/`，在 `manifest.json` 加一条（含 portrait/anchors/noise）
→ `pnpm eval -- --update` → 提交里说明加了什么、为什么。合成材料标 ✳️。

**改提示词 / 换模型**：改动 + `pnpm eval` 对照 + 基线更新**放同一个提交**，
提交信息里附数字。这与 `pnpm bench` 对引擎改动的要求同规格——数字不许口算、
不许只出现在聊天记录里。

**修了一个真实缺陷**：L2 必须加回归锁。锁要锁在**根因**上，不是锁在现象上——
例如上面那个提示词缺陷，锁的是"用户提示词不得出现『不要输出』"和"系统提示词把
『标题原样保留』写死"，而不是"某份材料的产出必须是某个样子"。

---

## 5. 现状与缺口台账

**已达标**：七个环节 L1 全部就位（③ 按设计豁免），L2 全部就位（每个环节都有对应
测试文件，见 §2 矩阵——这条由 `docs-contract.test.ts` 锁住，点名的文件不存在就红），
L3 覆盖 ⓪①（9 份合成材料 ✳️，112 个锚点），②④⑥ 有各自形态的跑分器。

> 这里刻意**不写单测总数**：那个数字每加一条锁就腐烂一次，而"数字对不对"本身没有信息量。
> 有信息量的是"每个环节都有锁"，那条能机械验证。

**第二轮发现（2026-07-29，分支收编，详见 RULES.md §4.10）**：

- **① 精简的 `formulaClean` 安全网有假阴性（已修）**：它借道 markdown-it-katex 的
  红字降级判定公式合法性，而**未知命令**不打 `katex-error` 类——AI 编造一个不存在的
  宏时闸会放行。这正是本标准 §0 说的"L1 闸本身写错，L2 抓不到"：假 AI 单测里
  `formulaLeakFake` 注的是 `$\frac{a}$`（老实现能抓的那一类），六个月都不会暴露另一类。
  新加的 `precheck-formulas.test.ts` 直接锁判据本身，不再只锁闸的调用行为。
  **下次 `pnpm eval` 若见精简采纳率下降，先按此归因**——闸变严了不是产出变差。
- **⑥ 排版新增 `pnpm qc:render`（门禁型）**：栅格化成品 PDF 查空白页与贴边，
  补上 `isAcceptableTrial` 的纯几何盲区。6 份 fixture 零误报，故可当硬门禁。

**首轮评测（2026-07-29，glm-4-flash）的发现**：

1. **精简恒定失效（已修）**：提示词与遮罩机制打架，0/9 块可用。修法是**结构性**的——
   标题不再遮罩成哨兵、改留自然 Markdown（实测：模型对句中哨兵保留率 8/8，
   对单独成行的标题哨兵稳定删除），新增 `checkHeadingsPreserved` 兜底"标题被改写"。
   修后 5/6 块采纳、压缩率 40~57%。两条回归锁 + 两条提示词内容锁已加。
2. **噪声剥除率偏低——归因已被推翻（2026-07-30）**：当时记为"提示词没被执行到位，
   属于调提示词的活"。换 `deepseek-chat` 后 **6/6 全部 100%**，锚点召回仍 100%、闸 6/6 通过。
   所以它**是模型能力问题，不是提示词缺陷**——弱模型执行不到"删掉与知识无关的噪音"。
   详见 RULES.md §4.16，那里同时记了由此产生的未定决策：eval 该钉在弱模型还是部署模型上。

**第三轮发现（2026-07-30，配好统一 key 后首次真实使用，详见 RULES.md §4.15~§4.16）**：

- **⓪ 结构化会编造内容（已修）**：18 字输入产出 160 字，把定义整段编出来。
  提示词里的「只重组，不新增知识」没管住。新增 `checkFabrication`
  （判据 `出 > 入*1.5+40`，只数公式区外的中文）并进既有追问循环，retry 3/3 收敛，
  六份材料零误报零额外轮次。界面新增红底分级提示。
  **这暴露了本标准的一处结构盲区：锚点召回率只测"有没有丢"，不测"有没有编"。**
- **eval 基线不记模型（已补 `_meta`）**：换模型会整体平移所有数字，不记模型就分不清
  "代码变了"和"模型变了"。首轮基线（glm-4-flash）就没记。

**缺口**：

- L3 的材料全是合成 ✳️。真实材料入库后证据等级才能升级（同 RULES.md §2.4 的口径）。
- ② 联网仍是观察型，没有 pass/fail 判据。它的产出依赖当次搜到什么，做精确对账会漂——
  要立标准需要先想清楚"哪些属性与搜索结果无关"（候选：总结里的数字是否都在片段里出现过，
  即确定性幻觉检测）。
- 无 CI。所有门禁都靠本地手工触发。
