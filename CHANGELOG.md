# CHANGELOG

这份文档收录 HalfHalf 已完成工作的**完整经过**——判例、数字、当场暴露的 bug、以及为什么这么选。
它从 README 的「开发计划 → 已完成」拆出来,因为那些条目每条都是一段研发笔记,留在 README 里会淹没
"这是什么、怎么跑"。

**为什么不压成一行**:踩坑记录是抄不走的东西(见 [PRODUCT.md](./PRODUCT.md) 的护城河一节)。
"改了什么"三个月后还能从 git log 里查;"为什么这么改、当时哪个判例逼出来的"删了就没了。

> 数值结论与阈值的权威出处是 [RULES.md](./RULES.md)(判例表 §1.6、证据等级、缺陷台账 §4);
> 这里记的是过程,那里记的是结论。两者冲突以 RULES.md 为准。

---

## 骨架期

- Monorepo 项目骨架搭建
- 前端界面(编辑器 + 参数面板 + 结果展示)
- 排版引擎核心实现(Markdown → HTML → PDF,含代码高亮/公式/图表/图片)
- 二分搜索算法与 SSE 集成
- PDF 下载端点(内存任务存储)
- AI 通用 BYOK 转发接口(占位,未接具体业务逻辑)
- 横竖版排版(portrait/landscape/auto,auto 并行跑两个方向取更优)
- 全仓 `tsc --noEmit` 修绿(补 DOM lib、Express 路由类型注解、字面量类型收窄),类型检查从此可当回归安全网用

**已删除**(2026-07-26 随旧多栏流管线一并移除,git 历史可寻回):确定性格式清理
`markdown-cleanup.ts`、单次渲染预览接口 `/api/render`、多栏流接口 `/api/optimize`。

---

## 排版引擎

### 图片链路(已通)

本地图片自动转 base64 内嵌(`md-to-html.ts` 的 `imageBaseDir`),绕开 file:// 相对路径失效的坑;
已用真实课件截图(MIS 能带图)验证渲染/缩放/宽高比/与文字块混排均正常。data:/http(s): 的 src
原样透传,为 web 上传铺路。

### 图片自成块 + 标准宽度吸附

分块器把独立成段的图片拆成 `kind: 'image'` 的内容块(图后剩余文字进"续块");测量阶段按图片自然
宽度吸附到能原尺寸放下的最小宽度档位(允许 5% 缩小容差,避免差几个百分点就跳大一档留出大片空白),
全放不下则取最大档由 CSS 等比缩小。列模式档位 = 1..N 栏宽,网格模式档位 = 8/12/16/24 格。
已验证:MIS 能带图列模式跨 2 栏、网格模式吸附 16 格(×0.99),均接近原尺寸显示、文字块绕排。

### 大文本压测(layout 引擎)

14 节操作系统讲义(表格/公式/密集要点混排)13pt 压进 2 页 A4 三栏、无超高块。压测暴露并修复了一个
真 bug:超宽公式 `applyAtomScaling` 缩放后仍被自身 `overflow-x` 裁剪(公式的溢出发生在盒子内部,
纸上没有滚动条;现在先把盒子放开到 max-content 再整体缩放)。

### 多栏排版

固定栏数 / auto 自动挑栏数,方向 × 栏数联合择优。需拿多种内容形态的真实样例调优(栏间留白 vs
内容完整性的取舍)。

### 网格版面模型(自动排版侧)

`grid-layout.ts` + `test/run-grid.ts`——内容区分割成 24 列单位格(A4 竖版格边长约 7.9mm),
块宽吸附标准档位 8/12/16/24 格(= 1/3、1/2、2/3、整页宽),块高向上取整到整数格(所有块落在格线上,
编辑器拖拽吸附用同一坐标系),块间强制留白烘进每块盒子(四周各内缩一半),永不贴边排满。

**默认留白 = 1 格宽(约 7.9mm)**——默认版疏朗优先,因为它不是最终结果,后面还有"放大"(保持留白
重搜字号)或编辑器人为微调;想更密可显式传更小的 `gutterMm`。复用同一套 分块→测量→skyline→渲染
流水线和目标页数二分搜索。

宽度档位默认 **6/8/12/16/24 格**(1/4~整页宽);文字块按**最大高宽比**(`maxAspect`,默认 2)选档
——小块吸窄档、"竹竿块"自动升宽档,不同体量的章节呈现不同宽度的卡片,避免全部挤最窄档输出千篇一律
的等宽栏(调大 maxAspect 趋向等宽栏,调小则更多宽卡)。8 种标准卡尺寸(迷你卡 6×6 → 通栏 24×6 格)
作为编辑器预设与 AI 内容长度目标导出(`STANDARD_CARDS`),自动模式不硬套固定高度——任意长度内容塞
固定高度卡片只会产生留白或溢出。

密度注记:疏朗默认版(1 格留白 + 混宽马赛克)比紧凑等宽版约让出 1~1.5pt 正文字号,由"放大"按钮和
编辑器收尾去榨回。

### 去掉纵向格线取整(目标页数优先)

原先块高会向上取整到整数格(理由是"块顶边落在格线上、方便编辑器吸附"),代价是每块最多白扔一格
(约 8mm)。实测微积分材料 19 个块因此膨胀 **32%**(内容占页 88% → 占位 116%),凭空多出一页,
且 PDF 上表现为块间大小不一的诡异空隙。

判断:编辑器的拖拽吸附是拖拽时现算的、不依赖自动版预先取整,用页数换几条对齐线不划算——纵向改为
紧密堆叠(盒高 = 内容高 + gutter),横向仍吸标准宽度档(那里的对齐才有视觉价值)。效果:块间距变成
恒定 gutter、版面填满;五个判例的字号/页数不变(都卡在 16% 跨不过的边界),收益体现在观感与留白
可控性上。

### 自适应分块细分

真实材料判例(微积分月考资料,`## 第N部分 + ### n.m 小节` 结构)暴露分块粒度问题——只按 `##` 切出
3 个整章巨型块,块是排版原子,字号被"最大块必须塞进一页"钉死、通栏巨块纵向堆叠留下大片空白。

修复:`chunkMarkdown` 加 `maxBlockChars`(默认 800),超长且内部有更深层标题的块按下一级标题递归
细分;纯标题行的"空章头块"并入下一块。效果:calc-monthly 从 5 块 → 19 块,2 页字号从"钉死在最大块"
恢复到正常搜索;四个既有判例结果不变。推荐器阈值同步校准(独立公式密度 ≥5/千字即理科公式——数学课
哪怕文本量大也不能推给会缩公式的 cram)。

### 拼装弃页修复(repack 页内换位 + backfill 跨页回填)

column-flow 原先宽块卡住即开新页、旧页缺口从此不回看(调试网格抓出的缺陷:12 格块卡住 → 下一页
空 2/3)。

- **repack(默认开)**:块卡住时把「当前页已放块 + 卡住块」按高度/面积序整页重排一遍,全装下才接受
  ——只在卡住时触发,"第 N 页装的是阅读顺序前缀"不变,页内位置本就是马赛克。实测 4/5 配置字号
  +0.5~1pt(真材料 test.md 6→7pt、politics 11.5→12.5pt);60 组随机压测不变量全过(无重叠/无丢块/
  不超页高/前缀性质),500 组统计从不劣于老行为。
- **backfill(默认关)**:后面的块可填进前面页的缺口,牺牲跨页阅读顺序换密度(随机压测多页文档平均
  省 22% 页数,1~2 页目标无增益)——是 RULES.md S2 的实现,走用户声明:前端「允许乱序换密度」
  复选框 → `/api/scene` 的 `allowReorder`。
- **evict 驱逐有意未做**(backfill 已拿走收益大头,等出现它才能救的判例)。

### 双面一张纸战役(2 页主场景的密度冲刺,2026-07-26)

第三份真材料 data-analysis(AI 生成学习材料,大表格 + 行内公式画像)一天校准出一整批:

- **窄边距** `marginMm`(6mm 白捡 7.6% 版面)
- **满版伸展** `stretchFill`(定稿后逐块字号 ≤+2pt 长满正下方空隙)
- **硬疙瘩联合选档**(拼装给高大块试更宽的档,测量 `bySpan` 白送数据零开销,悬崖 12.5→13pt)

同时修三缺陷:搜索假达标(超高截断不再算达标)、疙瘩候选漏真凶、行内公式溢出裸奔
(`applyAtomScaling` 第三类原子)。判例累计:默认 10.5pt/69% → **13pt/91%**。详见 RULES.md §4.6。

---

## 规则与判例

### cram 极限密度档

照 `samples/` 里真实半开卷小抄(手写金融工程、打印深度学习、荧光笔 OS 等 4 张)校准的规则集——
真实小抄的共同语言是"分隔靠线/颜色不靠留白、标题行内化、列表不缩进、重点靠视觉标记"。

实现为 `density: 'cram'`(行高 1.0 + 段距 0.05em)+ print.css 的 `[data-density='cram']` 作用域
规则(标题 ≤1.1em、列表缩进 1.05em、表格内边距减半、卡片发丝边框、多栏竖分隔线)。实测:14 节
OS 讲义 **1 页 A4 塞下且字号 11.5pt**(normal 档要 2 页)。顺带修了全局列表缩进(浏览器默认固定
40px,窄栏下吃掉近 1/5 行宽,改 1.3em 相对值)。

### 场景预设系统(分类讨论)

`scene-presets.ts` + `test/run-scene.ts`——不是所有课都该极致压缩,按内容对症分类:**极限文本**
(cram + 2mm 留白 + 窄栏,背诵型大文本)/ **理科公式**(minScale 0.85 公式几乎不缩、宁可升宽档,
去掉 6 格窄档,允许留白)/ **图文混排**(图片保原尺寸 + 紧凑文字)/ **均衡默认**(兜底)。

选择机制 = 内容特征统计(剥离公式/表格后的正文字数、独立公式密度、图片块占比)→ 确定性规则推荐
(理由可解释)→ 用户可改。判别口径的关键:只看**独立公式**(`$$`,刚性原子才需要保护),行内公式
随文字换行、不触发 formula 场景。四个 fixture 校准判例各归其位;实测对比:公式材料强制走 text-cram
时公式被缩到 ×0.51~0.74(大字文本配小字公式,正是要避免的形态),formula 预设下全程 ≥0.88。

### 硬约束取交集 + rule trace(规则引擎)

`rule-engine.ts` 落地 RULES.md §三——自动模式不再是"优先级链选一个预设当赢家、其余诉求整体丢弃",
而是 H1/H2/H4 逐条钳制取交集(minScale 下界取 max、密度上界取 min、窄档削除取并):"图 + 公式"
同时拿到 minScale 0.85 和 density compact,不再只保公式。

每条触发的规则记一笔 **rule trace**(接口返回、前端逐条显示),推荐理由由 trace 派生,替代与实际
参数脱节的手拼字符串。预设降级为命名快捷方式(用户强制指定时直接用)。对账:九个单触发判例与老预设
参数完全等价(零回归)。交集为空的放宽链(H1>H4>H3>H2)以当前约束词汇表为死路径,仅作规范预留。

### 学科层骨架(护城河的第一块砖)

`subject-rules.ts` 落地 RULES.md §二——`SubjectRule`(TS 强制 `evidence` 必填、指向具体材料)+
四条初始条目(微积分/操作系统/半导体电路/政治,自查只有微积分的证据是"经验观察"级,其余待真实材料
升级)。接通两条规则:

- **H3 表格可读**:声明表 = core 的学科(如 os)→ 表多时 minScale 抬到 0.7;politics 的表是
  support 不触发
- **S2 顺序弱**:声明 politics → 自动开跨页回填

学科识别 `suggestSubject` 按关键词命中给**建议**(test.md 命中微积分 6 词),前端提示"检测到可能是
X 课"——**建议 ≠ 声明**,用户在学科下拉选中才生效,识别错顶多退回力学层兜底。学科规则不得突破
力学层硬约束(地板之上活动)。

### 首份跨画像真材料入库 + 两个由它当场暴露的 bug

poli-econ(用户自有政经笔记,AI 辅助整理——正是产品主链路的真实输入形态)。特征 6572 字 · 独立
公式 32(4.9/千字)· 表 5,构成第九画像"大文本 + 公式 + 表格混合";公式密度距 H1 保护线恰差 0.1,
与 test.md(12.5/千字)从两侧夹逼公式线,5/千字首次获得真材料背书,阈值悬崖同时被实锤
(见 RULES.md §4.5)。当场修掉两个 bug:

1. **搜索兜底返回最差版本**——目标页数装不下时返回最小字号(6pt 小字 + 尾页 5/6 空的竹竿),改为
   "最少页数下的最大字号"(同判例 8pt 两页全满)
2. **推荐理由说瞎话**——有 32 个公式却说"无刚性原子",现在如实报告近线未达信息

回归测试 25 用例全绿。

### B1 模糊带双跑实测裁决(阈值平滑定案)

真实事故(用户 AI 精简正文后公式密度 4.9→5.9 跨线,场景 cram→formula、字号 8pt→6pt——减内容反而
变差)催生。公式密度落在 [3,8) 模糊带时规则引擎给出两侧候选参数,搜索层两套真跑、按**公式有效字号**
(正文字号 × 公式最小缩放,`formulaScale` 只算 katex-display、表格被缩不连坐)裁决:某侧不足另一侧
85% 出局,否则正文字号大者胜。

判据本身也被判例修正过一次(绝对缩放线被 poli-econ 证伪 → 改相对有效字号)。E2E:事故场景自动翻回
cram 8pt/2 页,无需手动改场景;带外零开销。43 用例全绿。

---

## AI 能力

### AI 语义级精简建议(`/api/ai/compress`)

产品第 ① 层(内容压缩)落地首版。分块后**遮罩刚性原子**——公式(`$$`/`$`)、代码(围栏/行内)、
表格、图片、标题全替换成 `〔HH数字〕` 哨兵,**只把纯散文交给用户自带 key 的 AI**(OpenAI 兼容格式)
改写成要点式,从源头杜绝"AI 改错公式"。

回填后过**三道安全网**:①哨兵完整性(原子逐一回来、不丢不重不杜撰)②公式预检(回填不引入原文
没有的 KaTeX 错误,复用 `precheck-formulas.ts`)③确实缩短——任一不过就作废该块、保留原文并给人话
原因。

**只出建议不自动改**:批量返回逐块「原文 vs 建议」+ 字数对比,前端展示 diff、用户逐块勾选,
「应用选中」按 range 降序拼回文本框,再走既有「生成 PDF」。BYOK 配置(端点/模型/key)存浏览器
localStorage,key 只在单次请求内存里过后端、不落服务器(同 `/ai/proxy` 姿态)。核心遮罩/回写/
安全网逻辑有单元测试(注入假 `chatComplete`,不花 token)。

遗留:v1 仅 OpenAI 兼容格式(Anthropic/Gemini 待适配器)、批量非流式、"合并空章头块"极少数情形
range 首行锚定可能落空则跳过该块回写。

### ⓪ AI 结构化入口(`/api/ai/structurize`)

任意粘贴内容 → 标准 .md(`#`/`##` 标题结构、每节 ≤800 字、公式 `$…$`、GFM 表格,**只重组不新增
知识**);服务器统一 key(env `HALFHALF_AI_*`,前端 BYOK 可覆盖);输入长度与频率限流
(`HALFHALF_AI_MAX_INPUT` 默认 6 万字符、`HALFHALF_AI_RATE_LIMIT` 默认每 IP 每小时 10 次)。
前端 `ChatIntake` 对话区,「采用并排版」一键进引擎。

配套 `/api/ai/chat`(SSE 多轮材料对话,服务端无状态)与 `/api/ai/providers`(服务商预设清单)。

---

## 导入与知识重构

> 这一批是第四阶段(PR-1/2/3)的产物。经过记录尚未补齐,以下按代码现状归纳,细节以
> [`packages/server/API.md`](./packages/server/API.md) 与源码为准。

- **文档导入**:`.docx` 转语义化 Markdown(mammoth);文字型 PDF 按页提取并插入「第 N 页」标记;
  图片沿用粘贴截图链路;扫描件返回 `OCR_REQUIRED` 与页数,不返回假成功。单文件 20 MB 上限,
  内存上传不落盘。
- **PDF 逐页质量路由**:按可测的坏字体比例决定每页走可编辑文字还是原页图像保真——异常页稀疏时只换
  那几页,密集时整篇统一视觉模式。不看文件名、学科或页数。
- **异步导入任务队列**:`POST /api/import/jobs` 返回 `202` + 任务号,配套查进度/取历史/取消/查限流;
  设 `HALFHALF_DATA_DIR` 后任务快照与结果可跨重启恢复,重启时未完成的任务标记 `IMPORT_INTERRUPTED`
  而非假成功。
- **网页导入**:`POST /api/import/url`(Readability 抽正文)。
- **KnowledgeIR + 可逆知识重构**:导入产物是带页锚点的知识节点;用户声明用途、必须保留、重点和希望
  忽略后,`/api/restructure/plan` 先出可审计计划(每条带来源锚点与理由,`mustKeep` 压过 `omit`,
  低置信度公式走 `visual_keep` 不被改写),`/api/restructure/materialize` 校验来源哈希后确定性应用,
  返回修订号、操作日志与前后对照,供前端做对比和撤销。

---

## 工程基建

### 工程三件套(回归网 / 并发 / 保真度)

1. **回归套件**:`pnpm test`(node:test + tsx,零新依赖)——拼装不变量随机压测、规则引擎判例对账、
   学科层行为锁、统计口径回归锁(表格误判/公式计数/代码围栏),秒级跑完不开浏览器
2. **并发控制**:browser-pool 信号量限制同时打开的 page 数(`HALFHALF_MAX_PAGES`,默认 3,
   FIFO 排队),多人同时生成不再互踩内存
3. **测量保真度修复**:`.hh-page` 是 BFC,首元素上边距计入测量高但渲染侧删掉了它——每块预留高度
   系统性虚增(实测 test.md 虚占 **9%**),表现为块间诡异空隙;测量/渲染两侧 CSS 规则严格对齐 +
   尾边距清零后版面明显更密(os-large 2 页 normal 12→13pt)。顺带 repack 细化:best-fit 落位 +
   钉住页首块,标题不再按尺寸序沉到页面底部

### 共享 Chromium 实例

`browser-pool.ts`:进程内只冷启动一次,测量/渲染复用、每次调用独立 page;大文本整轮字号搜索从
几十秒降到 2 秒内。脚本/服务收尾需调 `closeSharedBrowser()`。

### 公式预检

`precheck-formulas.ts`:不开浏览器把每块干跑一遍渲染管线,扫出 KaTeX 降级红字(`katex-error`)
并带块位置/错误信息上报,替代"到 PDF 里才看见红字"。也是 AI 改写公式的安全网(改写后再预检,
错了打回)。

### 网格调试视图

前端勾选「显示网格」(或 `/api/scene` 传 `debug: true`),PDF 上叠加 24 列 × 方格网格线、每个块的
方框和 `block-N·M格` 标签,一眼看清块边界/留白/档位选择。红框描在**盒子边界**
(`outline-offset` 外扩半个 gutter),所以标 N 格的块正好跨 N 个格子、相邻块的框相切,框与文字之间
那圈白就是 gutter 本身。叠加层只用 outline/背景/绝对定位标签,不参与布局计算——调试版与正式版排版
逐像素一致(有护栏脚本断言字号/页数相同)。

### Docker 化基础

Dockerfile / docker-compose / .env.example 入仓;访问口令中间件(`HALFHALF_ACCESS_CODE`,挡在所有
`/api` 之前,`/api/health` 放行);部署说明见 [`DEPLOY.md`](./DEPLOY.md)。
