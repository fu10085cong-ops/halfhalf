# 排版规则规范（RULES.md）

> 这份文档定义 HalfHalf 自动排版的**决策规则**：给定一份 Markdown，如何推导出排版参数
> （`minScale` / `density` / `widthTiers` / `strategy` / `gutterMm` / `maxAspect` 及回填、驱逐开关）。
>
> 它是 `scene-presets.ts`（四个场景预设 + 优先级链推荐器）的**继任设计**。现有代码是它的
> 粗糙前身，本文档指明目标形态；代码按此逐步收敛。**规则想错了改这份文档便宜，改代码贵。**

---

## 0. 为什么要两层

早期做法是「四个学科场景预设 + 优先级链推荐」。它有两个根本毛病：

- **优先级链首个匹配即赢**：一份「截图多 + 公式多」的半导体材料先命中 `visual`，公式就被
  `minScale 0.7` 静默缩小——冲突没有裁决，只有静默失败。
- **按学科名分类不封闭**：世界上的学科无穷多，四个筐永远装不下，新学科没有归宿。

解法是把决策拆成两层：

| 层 | 管什么 | 来源 | 能否被抄 |
|----|--------|------|---------|
| **力学层** | 内容里**能测出来**的物理事实：有多少刚性原子、多少柔性文字 | 确定性统计 | 算法可抄 |
| **学科层** | 力学层**测不出**的领域知识：顺序能不能打乱、哪类原子是核心、领域惯例 | 真实材料 + 用户反馈积累 | **积累抄不走** |

力学层保证**任何输入都有合理兜底**（新学科自动落到某个力学画像）；学科层是**护城河**
——它编码的是"数学推导顺序不能乱""政治课一问几面顺序无所谓"这类无法从文本可靠推断、
只能靠领域经验沉淀的事实（与 [README](./README.md) 「护城河」一节的判断一致）。

**关键约束（防止学科层变成后门）**：学科层只能做两件事——① 提供力学层测不出的**补充特征**
（顺序刚性、原子角色）；② 在力学层划定的**可行域内**给参数建议值。**学科层不得突破力学层
的硬约束**。这样两层永远不会打架：力学层保证下限，学科层锦上添花。

---

## 一、力学层

> **证据等级（下文各表通用记号）**——约束体系里"有据"和"拍脑袋"的东西混在一起，
> 不显式标出来，几个月后自己都分不清哪些数字能信、哪些随时该推翻：
>
> | 记号 | 等级 | 含义 |
> |------|------|------|
> | ● | 已校准 | 数值经真实材料/实测背书，**写得出出处** |
> | ◐ | 方向可信 | 机制方向有依据，**具体数字是拍的** |
> | ○ | 占位 | 纯假设，未经任何材料检验，随时可推翻 |
>
> ● 长什么样（有出处才算）：cram 密度档的行高/发丝线/标题行内化 ← 照四张真实小抄照片
> 逐项校准；图片档位 5% 吸附容差 ← 实测"差 1.3% 就跳大档、字号掉 6.5pt"修出来的；
> H1 的方向 ← 真实材料 test.md 的翻车现场（公式被缩到难读）。
> **◐ 升 ● 的唯一途径是真实材料**（见 §4.2）。

### 1.1 核心概念：刚性原子 vs 柔性内容

排版的一切困难都来自**刚性原子**——不能重新排版的东西：

| 类型 | 为什么刚性 | 放不下时只能 |
|------|-----------|-------------|
| 独立公式 `$$…$$` | KaTeX 渲染成固定二维盒子，无法折行 | 整体等比缩小 |
| 表格 | 列宽由内容决定 | 整体缩小 |
| 图片 | 像素尺寸固定 | 等比缩小 |
| 代码块 | 折行会破坏缩进语义 | 缩小或横向截断 |

**柔性内容**（正文、行内公式 `$…$`、列表）随容器宽度自由重排，任何宽度都排得好，
**不参与宽度决策**。

> 一句话：排版参数几乎全部由「这份材料有多少、多宽的刚性原子」决定。学科名只是这些
> 特征常见组合的俗称。

### 1.2 输入维度（每个都写清它在机械上影响什么）

**柔性量**
- `charCount`（剥掉公式/表格/代码/Markdown 标记后的正文字数）——决定"内容总量 vs 目标页数"
  的压力，进而决定 `density`。注意是**剥后口径**，比原始文件字数小得多。
- `inlineFormulaCount`、列表项数——计入柔性总量，但不单独触发任何硬约束。

**刚性原子（数量 + 宽度诉求）**——这是决策的主角
- 各类原子的**数量**和**密度**（个/千字）。
- 各类原子的**宽度诉求**：一个原子在参考字号下的自然宽度需要几格。公式塞不进 6 格就说明
  留 6 格档没意义。

**结构**
- `blockCount`、块长分布（中位数）——巨型块会钉死字号（见 [自适应细分](./README.md)），
  已由 `chunkMarkdown` 的 `maxBlockChars` 处理。

**顺序刚性（orderRigidity）——力学层测不出，占位待学科层填**
- 强：数学推导、代码教程（打乱=毁内容）。
- 弱：政治课"一问几面"、要点罗列（打乱几乎无代价）。
- 力学层默认**保守假定为强**（不敢乱动顺序）；靠学科层或用户声明放宽。

### 1.3 参数因果表

| 参数 | 由什么决定 | 机械原因 |
|------|-----------|---------|
| `minScale` | 刚性原子是否承载**核心信息** | 公式/代码是核心 → 缩小=毁内容 → minScale 高（0.85）；背诵课的表格缩小还能看 → minScale 低（0.5） |
| `density` | 柔性量 vs 目标页数 | 内容多、页数紧 → cram；宽松 → normal |
| `widthTiers` | **最宽**刚性原子的宽度诉求 | 公式普遍要 8 格 → 去掉 6 格窄档（留着只逼出缩放）；纯文字 → 保留窄档做马赛克 |
| `strategy` / `backfill` | **顺序刚性** | 顺序强 → column-flow 且不回填（保证跨页阅读顺序）；顺序弱 → 允许 size-desc / 跨页回填换密度。**页内换位（repack）不受此约束**：它只在块卡住时把当前页重排一次，"第 N 页装的是阅读顺序前缀"不变，变的只是页内位置——skyline 马赛克本就不保证页内严格线性 |
| `gutterMm` | 观感偏好 | **与内容无关**，纯审美。默认 1 格宽疏朗；cram 用 2mm |
| `maxAspect` | ⚠️ **无机械因果（缺口）** | 见 §四。目前是经验参数，实测非单调（os-large 在 2 和 1.2 各有局部最优） |

### 1.4 硬约束（先算，不可违反，冲突取最严）

每条硬约束把某个参数**钳到一个区间**。多条同时成立时，对每个参数取所有区间的**交集**
（下界取 max、上界取 min）。这是"图+公式混合""表格+公式混合"这类多刚性材料的正解——
不是选一个赢家，而是同时满足。

| 编号 | 触发条件 | 钳制 | 证据等级 |
|------|---------|------|---------|
| **H1 公式可读** | 独立公式密度 ≥ 5/千字 且 数量 ≥ 4；**密度落在模糊带 [3,8) 时静态判定只是倾向，由 B1 双跑实测裁决定案（§4.5）** | `minScale ≥ 0.85`；`widthTiers` 去掉 <8 格档 | 方向 ●（test.md 真实翻车）· **密度线已被两份真材料夹逼**：4.9/千字应不触发（poli-econ，cram 下短公式可读）< 线 ≤ 12.5（test.md，必须保护），5.0 落在区间内成立 · 钳制值 ◐ |
| **H2 图片保真** | 图片块 ≥ 2 或 占比 ≥ 15% | `minScale ≥ 0.7`（沿用 visual 预设先例）；`density ≤ compact` 给图让位；图片档位吸附容差收紧 | 方向 ◐ · 数值 ◐（仅档位吸附容差 5% 是 ●） |
| **H3 表格可读** | 表格数 ≥ 3 且 表是核心（学科层 `atomRoles.table === 'core'`，须用户声明学科） | `minScale ≥ 0.7` | 机制已实现（2026-07-20）· 方向 ◐ · 数值 ○（无任何真实材料检验过"表核心的课该怎么排"） |
| **H4 代码不折行** | 代码块 ≥ 3 | `minScale ≥ 0.75`；代码块 `white-space` 不强制换行 | 方向 ◐（折行毁缩进是领域常识）· 数值 ◐（零真实编程材料） |

**取交集已落地**（2026-07-20，`rule-engine.ts`）：自动模式不再是"优先级链选一个预设"，
而是逐条钳制取交集——"图+公式"同时拿到 `minScale 0.85`（H1）和 `density compact`（H2），
对账脚本验证九个单触发判例与老预设完全等价（零回归）。多类刚性原子并存时返回提示
（保护已同时生效，但空间必然更挤），不再有静默丢弃。

**交集为空时的失败行为（必须显式，不能静默）**：按优先级 H1 > H4 > H3 > H2 放宽最低优先级
的那条，并在 rule trace 里记警告。注意：以当前约束词汇表（minScale 只有下界、密度只有
上界、档位只削窄档，各参数的界全部同向）**交集不可能为空**，这条链目前是死路径——
留给将来出现反向界（如"密度不得紧过 normal"）时用。**链的排序本身是 ○**——
从未有一份材料同时踩过三条线来检验它对不对。

### 1.5 软偏好（在硬约束可行域内择优）

| 编号 | 条件 | 偏好 | 证据等级 |
|------|------|------|---------|
| **S1 密度优先** | 柔性量大、刚性原子少 | 往 cram 靠，gutter 收小 | 方向 ◐ · 大文本线 1500 字 ◐（cram 档本身是 ●） |
| **S2 允许乱序换密度** | 顺序刚性弱（学科层/用户声明） | 开启 size-desc / 跨页回填 | ◐（2026-07-20 起两个入口都通：前端「允许乱序换密度」→ `allowReorder`；或声明学科且其 `orderRigidity: 'weak'`（politics）→ 自动开回填。随机压测 500 组平均省 22% 页数，但**收益集中在多页长文档**，现有 1~2 页判例无增益；缺真实材料验证） |
| **S3 疏朗优先** | 目标页数宽松（内容远小于容量） | normal 密度，gutter 用满 1 格宽 | ◐（纯审美偏好，谈不上校准） |

### 1.6 八画像判例表

特征值取自 `test/fixtures/` 实测（`test/dump-features.ts`，一条命令全量速查）。
**期望输出**是按本规范推演的结果；**当前输出**是引擎现在的实际行为；两者不一致处标 ⚠️ 记为待办。

对账已固化为回归测试 `test/unit/rule-engine.test.ts`（前身是 scratchpad/reconcile.ts）：**七个现有 fixture 的规范预测与引擎实际
输出全部一致**——达成"只读本文档即可预测输出"的判据。

| 画像 | 判例 fixture | 实测特征 | 触发硬约束 | 期望/实际 |
|------|-------------|---------|-----------|----------|
| 纯文本密集 | os-large | 1683字 · $$2(1.2/千) · 真表2 | 无（公式稀疏） | cram ✓ 一致 |
| 公式主导 | formula-heavy | 203字 · $$9(44/千) | H1 | 公式档 ✓ 一致 |
| **公式+大文本混合** | calc-monthly（复刻）/ **test（真实）** | 2297字·$$13 / 2248字·$$28 | H1（公式赢过大文本 cram 线） | 公式档 ✓ 一致（但省页收益待回填/驱逐） |
| 图密集 | image-test | 301字 · 图1(占比20%) | H2 | 图文档 ✓ 一致 |
| **图+公式混合** | image-formula ✳️ | 406字 · $$4(9.9/千) · 图2 | H1 ∩ H2 | **交集生效**（2026-07-20）：minScale 0.85 ∩ density compact 同时成立 + 并存提示 ✓（老优先级链只保公式，图片仅拿到一句警告） |
| **代码密集** | code-heavy ✳️ | 566字 · $$0 · 码5 | H4 | 代码档（minScale 0.75、去 6 格窄档）✓ 一致 |
| 代码+公式混合 | sample | 794字 · $$5 · 码5 | H1 ∩ H4 | **交集生效**：minScale = max(0.85, 0.75) = 0.85、去窄档取并 + 并存提示 ✓（此例交集恰与公式档参数重合） |
| **表格密集** | table-heavy ✳️ | 672字 · **表5** · $$0 | H3（须学科声明） | **H3 机制已通**（2026-07-20）：声明表=core 的学科（如 os）→ minScale 抬到 0.7 ✓；声明表=support 的学科（politics）→ 不触发 ✓。⚠️ 默认（不声明学科）仍判"均衡"——力学层测不出"表是不是知识本体"，这是设计而非缺陷；真缺口是**没有一条表=core 学科的真实材料**（table-heavy 是经济学判例但无 econ 规则） |
| **弱逻辑并列文字** | politics ✳️ | 1895字 · 无刚性原子 | 无（柔性为主） | cram ✓ 一致；S2 两个入口都通（勾「允许乱序换密度」或声明学科 politics→orderRigidity weak），但 1 页目标下 backfill 无增益（11.5→12.5pt 的提升来自 repack）；不声明时仍保守假定顺序强（设计如此，不赌启发式） |
| 悬崖跨线（B1 裁决） | poli-econ-slim ✳️派生 | 5705字 · $$32(5.6/千) · 表5 | 带内：静态倾向 formula，B1 双跑 | **B1 自动选对**：formula 7pt/3页（公式有效 7.0pt）vs cram 8pt/2页（6.3pt）→ 差距 <15% 打平 → cram 胜出 ✓——2026-07-23 事故场景从此无需手动改场景 |
| **大文本+公式+表格混合** | **poli-econ（真实 ②）** | 6449字 · $$32(4.96/千，剥来源注释后口径，见 §4.6④) · 表5 | 无——公式密度差 0.04 未过 H1 线 | cram ✓ **输出实际可读**（全是 m′=m/v 类短公式，窄档塞得下不用缩）；2 页 8pt；1 页物理不可达（兜底改给 8pt/2 页）；乱序无增益。**阈值悬崖实锤**：再多 1 个公式整档跳 normal，见 §4.5 |
| **大表格+行内公式+要点式** | **data-analysis（真实 ③）** | 3671字 · $$0 · 表1（257mm 大表） · 16块 | 无——公式全用 $...$ 行内写法，$$计数为 0（正因如此暴露 §4.6③ 行内公式裸奔） | text-cram ✓；**2 页（一张纸双面）目标的全套密度机制校准材料**：乱序 10.5→11.5pt/69→81%（**S2 在 2 页目标的首个真收益**——politics 行"backfill 无增益"仅限 1 页目标）、窄边距 6mm 再 +0.5pt、联合选档 12.5→13pt/91%（大表格就是悬崖真凶）、满版伸展 5 块 +0.5~2pt。暴露三个缺陷并当场修掉，见 §4.6 |

> ✳️ = **合成判例**（synthetic）。它们只用于**锁定行为**（"5 个代码块必须触发 H4"这类确定性
> 逻辑），**不得用于校准阈值**——拿想象出来的材料调阈值等于自己跟自己对答案，calc-monthly
> 已经翻过一次车（公式数比真实材料低估一半）。阈值校准必须等真实材料。

> **真实材料 test.md 揭示的两件事**（2026-07-15 用户提供）：
> ① 它有 **28 个独立公式**，我的复刻 calc-monthly 只有 13 —— 自造判例把公式量低估了一倍多，
>    之前基于复刻做的调参都偏松。**调参不能只靠自造 fixture。**
> ② 它只有 **1 个真表格**，但 `tableCount` 报 6（5 条 `---` 水平线被误判）—— 见 §4.1(2)。

---

## 二、学科层

### 2.1 学科规则能表达什么、不能表达什么

**已落地**（2026-07-20，`subject-rules.ts`，结构与下面一致，另加前端显示名 `name`）：

```
SubjectRule {
  id: string                       // 'calculus' | 'os' | 'politics' | ...
  aliases: string[]                // 关键词，用于"建议"识别（非强制）
  // ① 力学层测不出的补充特征：
  orderRigidity?: 'strong' | 'weak'   // weak → S2 自动开跨页回填
  atomRoles?: { table?: 'core'|'support', image?: ..., formula?: ... }  // table core → H3 生效
  // ② 可行域内的参数建议（不得突破力学层硬约束）：
  suggest?: Partial<{ gutterMm, maxAspect, ... }>
  evidence: string                 // 这条规则基于什么真实观察，必填，可追溯（TS 强制）
}
```

**能**：补充顺序刚性、标注原子角色（政治课的表是 support、操作系统的对比表是 core）、
在可行域内建议观感参数。
**不能**：把 `minScale` 降到 H1 要求以下、关掉图片保真——**硬约束是地板，学科层只能在地板以上活动**。

### 2.2 学科识别

沿用「程序推荐 + 用户可改」：`aliases` 关键词命中给出**建议**，用户在前端可确认或改选。
识别错了顶多退回力学层兜底，不会出灾难。**永远不把学科识别当作硬输入。**

已落地：`suggestSubject` 按命中数取最强建议 → 接口返回 `subjectSuggestion` → 前端提示
"检测到可能是 X 课"；只有用户在学科下拉里选中（`subject` 字段）才生效。
实测：test.md 命中微积分 6 个关键词、politics.md 命中马原——建议不声明就不改变任何参数。

### 2.3 初始条目（本次会话已积累的真实观察，全部标注证据；已落码 `SUBJECT_RULES`）

| 学科 | orderRigidity | 原子角色 | 关键观察（evidence） |
|------|--------------|---------|---------------------|
| 微积分 calculus | strong | formula=core | 真实材料 test.md：`\qquad` 在一个 `$$` 里并排 2~3 个公式，是宽度失控主因（泰勒公式块被推到 12 格卡死整页）；28 个独立公式 |
| 操作系统 os | strong | table=**core** | os-large：进程/页面置换对比表是考点本体，不能缩 |
| 半导体/电路 | strong | image=core, formula=core | 用户原话"图片优先保原尺寸"；图+公式混合的典型 |
| 政治 politics | **weak** | table=support | 用户原话"一个问题几个方面"，方面之间逻辑弱，可乱序（S2）。⚠️ 真材料 poli-econ 提出**反例线索**：政治经济学笔记自带"商品—货币—剩余价值—…"逻辑主线，顺序未必弱——weak 判定适用于"一问几面"要点型（马原/毛概），政经类应谨慎；且它 $$32 · 表5，`table=support` 对政经也存疑 |

### 2.4 维护流程

**数据存哪**：`scene-presets.ts` 旁边的 TS 常量（和 `SCENE_PRESETS` 同一模式）。
不搞 JSON/数据库——条目封顶 20~30 条，是**配置不是数据**；TS 能强制 `evidence` 必填，
JSON 还得自己写校验。等真有外部贡献者、或条目上百时再谈迁移。

**证据分两类，只有第二类是护城河**：

| 类型 | 例子 | 是护城河吗 |
|------|------|-----------|
| 领域常识 | "数学推导顺序不能打乱" | ❌ 学过的人都能写 |
| **经验观察** | "微积分资料爱用 `\qquad` 在一个 `$$` 里并排 3 个公式，是宽度失控主因" | ✅ **必须见过真材料** |

> 自查：§2.3 的四条初始条目里，**只有微积分那条属于第二类**（来自 test.md）。
> 真实进度比条目数看起来的少。

**一条规则的生命周期**（锚定材料，不是锚定想法）：

```
① 拿到真实材料（朋友的课，跨专业）
② 跑一遍，看输出哪里不对
③ 守门问题：「不知道这是什么课，这个问题还能被发现吗？」
     能  → 力学层的活（补统计/调阈值），禁止碰学科层
     不能 → 才轮到学科层
④ 材料脱敏后存成 fixture + 规则带 evidence，一起提交
⑤ 判例表加一行：没有 fixture 能证明它有用的规则 = 迷信
```

**第③步是纪律核心**。学科名是个筐、什么都能往里扔——不设这道闸，学科层几个月后就会
变成一堆"我觉得数学课该这样"的意见集合，那不是护城河是负债。
反例：`\qquad` 并排公式导致宽度失控——**这条不该进学科层**，因为力学层测得出
（"某个 `$$` 自然宽度 > 12 格"）。学科层只记录测不出的（顺序刚性、原子角色）。

**防退化三条硬规矩**：
1. `evidence` 必填且指到具体材料——写不出"哪份材料、什么现象"就是猜的。
2. 每条规则必须能被证伪——答不上"没有它会怎样"就别加。
3. 规则与 fixture 同生共死——删材料就得复查规则。

**朋友材料的入库边界（材料主要来源，必须先立规矩）**：
- **先问再用**：入库前取得明确同意，说清"会进开源仓库、公开可见"。
- **脱敏**：删掉姓名、学号、班级、联系方式。`samples/` 里那四张照片就含真实姓名+学号
  与小红书号，已在 `7.16.1` 提交进仓库——**推远程前应处理**（见 README 的口碑/信任论述）。
- **学术诚信**：只收"自己整理的知识点总结"，排除历年真题/答案/未公开课件原文
  （与 README「学生内容社区」一节的红线一致）。
- **来源标注**：fixture 顶部注释写清来源与授权状态（真材料 / 合成 ✳️）。

### 2.5 反馈回路与隐私边界

- **信号**：用户把自动推荐改成别的，就是一次"这个画像判错了"的反馈。
- **隐私红线**：优先靠**开发者自行收集真实样例** + **用户主动提交的反馈**，
  **不默认采集用户内容**。README「学生口碑/信任」是核心资产，一次"偷偷上传我的复习资料"
  级别的事件会直接透支它。宁可积累慢，不可越界。

---

## 三、自动模式决策流程

```
1. 算力学特征           analyzeContent                          ✅ 已实现
2. 叠加学科补充特征      若用户声明/识别出学科，取 orderRigidity、atomRoles   ⬜ 等学科层
3. 算硬约束 → 可行域    H1/H2/H4 逐条钳制，取交集（rule-engine.ts）          ✅ 已实现（H3 等学科层）
4. 软偏好 + 学科建议择优 在可行域内应用 S1~S3（S2 走用户声明 allowReorder）   ✅ 已实现（学科 suggest 等学科层）
5. 输出参数 + rule trace 每条触发的规则记一条账，理由由 trace 派生            ✅ 已实现（接口返回 trace，前端逐条显示）
6. 用户可强制预设覆盖    预设降级为"常见组合的命名快捷方式"，规则引擎才是真身  ✅ 已实现
```

**rule trace 已取代手写 reason**（2026-07-20）：过去 `recommendScene` 每个分支手拼一句
中文理由，与实际生效的参数脱节；现在 trace 是"实际触发了哪些规则 + 各钳了什么"的忠实
记账，reason 由它派生，不会说谎。`recommendScene` 保留为规则引擎的薄包装（旧签名）。

---

## 四、已知缺陷与缺口（如实记录，不装完备）

### 4.1 代码里查实的 bug（2026-07-15 已修，验证见 `scratchpad/verify-fixes.ts`）

1. ~~**`analyzeContent` 不统计代码块**~~ → **已修**：`codeBlockCount` 先数再摘围栏。
   实测 sample.md=5、random-topic=3。新增 `code` 预设（minScale 0.75、去 6 格窄档）实现 H4。
2. ~~**`tableCount` 把水平线误判成表格**~~ → **已修**：判定改为"整行只由 `| - : 空白` 组成
   **且至少各有一个 `|` 和 `-`**"（`isTableDivider`）。test.md 从误报 6 张回到真实的 1 张；
   `---`、`----------`、`- - -` 均不再误判。
3. ~~**死字段**~~ → **基本修完**：`codeBlockCount` 参与 H4；`tableCount` 自 2026-07-20 起
   参与 H3（学科声明表=core 时）；仅 `inlineFormulaCount` 仍只用于展示（行内公式随文字
   换行、无需保护，这是口径而非缺陷）。
4. ~~**优先级链 = 静默失败**~~ → **已彻底修**（两步到位）：
   ① 2026-07-15 先把静默失败改成"优先级链 + 警告"（冲突不再无声）；
   ② 2026-07-20 落地 §1.4 目标形态的**硬约束取交集**（`rule-engine.ts`）——多类刚性原子
   的钳制同时成立（minScale 下界取 max、密度上界取 min、窄档削除取并），并输出 rule trace。
   对账：九个单触发判例与老预设参数完全等价（零回归），image-formula 拿到 0.85∩compact，
   sample 的交集恰与公式档重合。优先级链只剩"交集为空时的放宽顺序"一个角色，
   且以当前约束词汇表为死路径（见 §1.4）。
5. ~~**弃页缺陷**~~ → **已修**（2026-07-20，不变量已固化为 `test/unit/pack-invariants.test.ts`，A/B 用 `test/run-ab.ts` 可复跑）：
   column-flow 里宽块卡住即开新页、旧页缺口从此不回看，最坏一页空 2/3。两个补丁：
   - **repack 页内换位（默认开）**：卡住时把「当前页已放块 + 卡住块」按高度/面积序重排一遍，
     全装下才接受。顺序语义无损（页成员仍是阅读顺序前缀）。实测 4/5 配置字号 +0.5~1pt
     （真材料 test.md 6→7pt、politics 11.5→12.5pt）；60 组随机压测不变量全过（无重叠/
     无丢块/不超页高/前缀性质），500 组统计平均省 3.7% 页数、从不劣于老行为。
   - **backfill 跨页回填（默认关）**：S2 专属，见 §1.5。
   - **evict 驱逐未做**（有意）：backfill 已拿走"填旧页缺口"的收益大头，驱逐只是换哪个块
     进缺口、还多一层顺序破坏，等出现它才能救的判例再说。
   - **同日两处细化**：repack 内部的落位改 **best-fit**（最低落点优先；leftmost 会把后续块
     全勾在最左凸台上堆成左塔），并且优先尝试**钉住页首块**的排序（标题这类短小块不再按
     尺寸序沉到页面底部）。代价：os-large cram 1 页的 +0.5pt 让渡回基线 11.5（观感换密度，
     单配置）；os-large 2 页 normal 反而升到 13pt（配合下面第 6 条的保真度修复）。
6. ~~**测量高 ≠ 渲染高**~~ → **已修**（2026-07-20，诊断脚本逐块量化）：`.hh-page` 是
   multicol 容器（BFC），首/尾子元素外边距计入测量高；但渲染侧 `.layout-block > :first-child
   { margin-top: 0 }` 只在渲染侧删首边距——**每块预留高度系统性虚增一截标题上边距**，
   实测 test.md 19 块虚占 **9%**（首边距 62mm + 尾空白 16mm / 共 872mm），版面上表现为
   块间大小不一的诡异空隙。修法：测量侧补同名规则对齐，尾边距两侧一并清零（块间分隔由
   gutter 负责，盒内尾部空白是纯浪费）。测量/渲染的块级 CSS 从此必须逐条对齐，不齐必错。
7. ~~**目标不可达时返回最差版本**~~ → **已修**（2026-07-20，poli-econ 暴露）：目标页数
   物理上装不下时，搜索直接返回**最小字号**的尽力结果——6572 字填 1 页目标，用户拿到
   6pt 小字 + 第 2 页只剩一根竹竿（5/6 空白）。修法：探测到不可达就把目标改成"实际能做到
   的最少页数"继续二分，交出**最少页数下的最大字号**（同判例变为 8pt、两页全满）；
   `withinTargetPages` 仍按用户原始目标如实报 false。


### 4.2 判例覆盖：十画像已有判例，真材料三份

2026-07-15 补齐四个合成判例（code-heavy / image-formula / table-heavy / politics），
八画像全部可跑；2026-07-20 入库**第二份真材料 poli-econ**（用户自有政经笔记，经 AI
辅助整理成 Markdown——恰是产品主链路的真实输入形态），带来第九个画像"大文本+公式+表格
混合"。2026-07-26 入库**第三份真材料 data-analysis**（用户提供的 AI 生成学习材料——
⓪ 结构化入口时代最典型的输入形态），带来第十个画像"大表格+行内公式+要点式"。
当前 **13 个 fixture 里有三份真材料**（test.md 微积分、poli-econ 政经、data-analysis），其余合成。

poli-econ 是**第一份真正的阈值判例**：与 test.md 一起从两侧夹逼公式密度线
（4.9 应不触发 / 12.5 应触发），H1 的 5/千字第一次有了真材料背书（见 §1.4 证据列）。

真实材料 test.md 已经证明过合成判例的危险：我的复刻 calc-monthly 把公式数低估一半
（13 vs 28），基于它做的调参全部偏松。

**因此本仓库的判例分两类，用途不可混淆**：

| 类型 | 用途 | 能不能用合成的 |
|------|------|--------------|
| **行为判例** | 锁死确定性逻辑（"5 个代码块 → H4"） | ✅ 能，合成足够 |
| **阈值判例** | 校准数值（"代码块 ≥4 才算密集"） | ❌ **必须真材料** |

现有合成判例全部只承担第一类职责。**§1.4 的所有阈值仍是待校准状态**，等真实材料到位。

换成证据等级（§一 图例）的说法：**◐ 升 ● 的唯一途径就是阈值判例，即真实材料**。
每拿到一份新学科的真实总结，预期能把 1~2 个数字从 ◐ 变 ●——这也是"找朋友要跨专业
真材料"排在待办首位的原因。

### 4.3 无机械因果的参数

`maxAspect` 目前是纯经验值（证据等级 ○），且实测非单调（os-large 在 maxAspect=2 给 11pt、=1.2 反给 11.5pt，
因 12 格卡两块正好铺满一行、接缝更少）。规范暂无法给出它的因果规则，标记为**待研究**。

### 4.4 顺序刚性无法自动测

启发式（连接词密度、"一二三"并列 vs "步骤1/2"）都不可靠。**方案：力学层默认保守（顺序强），
由学科层或用户在前端显式声明放宽。** 不赌启发式。

两个声明入口均已落地（2026-07-20）：① 前端「允许乱序换密度」复选框 → `/api/scene` 的
`allowReorder`；② 学科声明（`subject: 'politics'` → `orderRigidity: 'weak'` 自动放宽），
配套关键词识别建议（建议 ≠ 声明）。缺的只剩"更多学科条目 + 真实材料校准"。

### 4.5 阈值悬崖 → B1 模糊带双跑裁决（2026-07-23 定案落地）

曾经的问题：阈值是硬边界，差 0.1 就整包换参数。两次真材料实锤：
① poli-econ 距 H1 线恰差 0.1（4.9/千字）；② 用户 AI 精简正文后密度 4.9→5.9 跨线，
场景 cram→formula、字号 8pt→6pt——**减了内容反而字变小**，悬崖不再是假想。

**定案（路线三，engine/adjudicate.ts）**：密度落在模糊带 **[3, 8)** 且数量达标时，
规则引擎给出两侧候选（保护公式 / 要密度），搜索层**两套都真跑**、按实测裁决：

- 判据 = **公式有效字号**（正文字号 × 公式最小缩放，`formulaScale` 只算 katex-display，
  表格被缩不连坐）。某侧不足另一侧 85%（复用 H1 钳制值）→ 出局；否则比正文字号、平比页数。
- 带外零开销单跑；带内多花一轮搜索（约 +2s）；带边界数值 ○ 占位待校准。
- **为什么裁决而不是插值/滞回**：离散参数没法插值、中间态判例锁不住；滞回треб状态且
  只治编辑翻转。裁决的本质是把"公式会不会被挤坏"从静态猜测（密度代理）换成**测量事实**
  ——顺带治了"密度只是宽度的粗代理"（§1.4 H1 注）。
- **判据本身也被判例修正过一次**：第一版用"绝对缩放 ≥0.85"否决线，被 poli-econ 证伪
  （cram 有个宽公式 ×0.78 被判出局，换来的"保护"是 6.5pt/3 页——公式有效字号只多 0.3pt，
  正文和页数全赔）。改为相对有效字号后两侧真判例全部归位（见 §1.6 判例表）。

E2E 判例（/api/scene 自动模式，目标 2 页）：poli-econ → cram 8pt/2页（B1 确认）；
poli-econ-slim → **B1 翻回 cram 8pt/2页**（静态倾向 formula 被实测推翻）；
test.md（12.5/千字，带外）→ formula 单跑，保护如旧。

### 4.6 双面一张纸战役：三个缺陷 + 两个机制（2026-07-26，data-analysis 判例，全部已修/已落码）

场景定调：**考场只允许带一张纸 → 2 页（正反面）是主战场**，"降到 1 页"不是末页空白的解。
一份真材料一天连暴三个缺陷、催生两个机制，全程见判例表 data-analysis 行：

① **搜索假达标**（已修，`isAcceptableTrial` + 回归锁）：搜索只看"页数 ≤ 目标"，
   横版 12.5pt 表格超页高、Tableau 一整行被裁仍被选为最优——**内容完整从此是达标的
   必要条件**；唯一例外是最小字号就超高的块（字号救不了，退回尽力交付 + 警告）。

② **硬疙瘩卡页 → 联合选档**（已修，`packWithNuggetVariants`，默认开）：块先选档、
   拼装不商量——257mm 大表格在 13pt 把 2 页顶成 3 页，而它换 16 格只有 105mm。
   现在拼装给"高过内容区 45%"的高大块试更宽的档（测量 bySpan 白送数据，零浏览器开销），
   页数严格变少才采用。**候选不能只取最高的前 2**：190/187mm 高散文块比表格更高却救不了
   页数——真凶往往不是最高的（回归锁已加）。悬崖 12.5→13pt，填充 87→91%。

③ **行内公式裸奔**（已修，applyAtomScaling 第三类原子）：`$...$` 行内长公式不参与
   H1 保护（$$ 计数为 0）、不参与原子缩放、也不能换行——皮尔逊公式分母被页边裁掉。
   现在真溢出的行内公式按"公式左缘到容器右缘"的可用宽度缩放，系数计入块的选档保护。
   **上游根治在 ⓪ 入口**：structurize 的 prompt 明令独立公式用 $$...$$（结构化过的
   材料不会再有此形态）；本修复是没走 ⓪ 的材料的兜底保真网。

④ **来源注释污染判例**（已修，`stripHtmlComments` + 回归锁）：markdown-it 在
   `html:false` 下把 `<!-- -->` 注释**当文本渲染**、字数也计入正文——poli-econ 的
   来源注释一直躺在判例统计和渲染里。现在 chunkMarkdown 入口剥掉全部 HTML 注释
   （围栏内的除外，那是代码内容）。口径微调：poli-econ 6572→6449 字、
   密度 4.9→4.96/千——**判定与 B1 带内行为均不变**，"差 0.1 未过线"收紧为"差 0.04"，
   两侧夹逼的校准结论如故。

两个机制（都有默认开/可关的参数，老行为可复现）：
- **满版伸展**（stretchFill）：定稿后逐块字号 +0.5pt 步进（≤+2pt）向下长满"正下方
  空隙"——柱底锯齿换成更大的字。空隙只属于上方块，逐块独立互不冲突。
- **窄边距**（marginMm）：6mm 比默认 10mm 白捡约 7.6% 版面。

**末页余量的边界（诚实记录）**：内容量 1.6 页时第 2 页天然 ~80%，伸展上限 +2pt 吃不掉
大空洞、空柱横向无人能长。剩余选项 = AI 精简继续减量 / 末页均衡重排 /
末页填充物（未做）/ 留作手写补充区。→ 末页均衡重排已在 §4.7 落地
（末页拉宽重排 + 末页伸展上限 +4pt），data-analysis 末页实测 91→97%。

数字账（data-analysis，2 页目标累计）：默认 10.5pt/69% → 乱序 11.5/81% → +6mm 12/82%
→ AI 精简口径 12.5/87% → 联合选档 **13pt/91%**，伸展块最高 15pt，零截断、公式完整。
（§4.7 后同条件复测：**13pt/98%+97%**，字号持平、两页近满。）

### 4.7 版式保真战役：五个缺陷 + 五个机制（2026-07-26，cs-programming 判例，全部已修/已落码）

第一份编程课材料（cs-programming ✳️，此前该画像零材料）在网页端一跑就连暴五个缺陷
——截图判例：快排代码折出孤行 `1;`、复杂度表"不稳"竖折、H1 标题沉到页中、
页 1 右上 8 格 × 139mm 死洞（17% 页面）、末页填充 55%。逐个钉死：

① **代码折行毁语义——H4 名不副实**（已修，print.css `pre{white-space:pre}` +
   applyAtomScaling 第四类原子）：`pre-wrap + break-word` 允许代码任意折行——
   `i = lo - 1;` 折出孤行 `1;`，if 体内的 swap 折行后顶格看似循环外语句，行尾注释
   折行后残句看似代码。H4 只删窄档挡不住（13.5pt 最长行 97mm > 12 格内容宽 87mm）。
   现在代码与表格/公式同权：禁折行，超宽整体等比缩小，系数走 data-hh-scale → 块
   scale → minScale(0.75) 选档保护。**保真的代价要认**：代码不再靠折行"作弊省宽"，
   宽度需求变真，cs 基准字号 13.5→13pt——换来的是语义零损伤。

② **紧凑表竖折——表格原子保护形同虚设**（已修，applyAtomScaling 紧凑表 nowrap
   探针）：表格原子缩放只在"溢出容器"时触发，CJK 单元格会先折行、永远不溢出——
   速查表"不稳"折成竖条。现在单元格全 ≤16 字的"紧凑表"先禁折行量自然宽：装得下 →
   保持不折（零成本）；缩到 ≥0.75 可救 → 交给通用原子缩放；太宽 → 退回折行但把
   "假想缩放"记进 data-hh-scale 逼选档升宽档。单元格像句子的散文表不碰——折行是对的。

③ **洞驱动降档**（新机制 holeFill，默认开，联合选档的对偶）：块的档位测量时定死，
   16 格块所在行必然剩 8 格、没有块能降档来填——死洞还在页面上方，伸展（只向下长）
   也吃不到。现在块卡住、页内换位也救不回时，按 bySpan（零开销）试更窄的档塞进
   本页缺口，可读性 scale ≥ minScale 才算数。cs 实测：四、栈与队列 12→8 格进洞。

④ **repack 高度序打散阅读序——标题沉页中**（已修，repack 增加阅读序优先排列）：
   主流程 leftmost-first-fit 爱把块竖着堆成左塔 → 宽块卡住 → 老 repack 只试
   高度序/面积序，131mm 的"三"排到 H1 标题前面。其实**按阅读序改用 best-fit 重放
   往往就装得下**——顺序一分不牺牲。现在阅读序是第一优先排列（回归锁已加）。

⑤ **末页拉宽重排 + 末页伸展上限**（并入 stretchFill）：末页是量化余数的倾倒场，
   16+12 混档留整列死柱（cs 末页 35%）。多页结果且末页填充 <60% 时：末页文字块
   统一升通栏（bySpan、scale 达标）、按阅读序堆叠、余量均摊成块间呼吸位——每块
   下方都有了自己的空隙，随后伸展逐块吃掉；末页伸展上限 2→4pt（下方多为页底空白，
   字号差异顾虑小）。伸展同时修了档位错位：降档/换档/拉宽块按**落位档**查 bySpan
   高度（此前重测选档漂移就放弃伸展），且大字号下 scale 掉出下限即到顶。

数字账（cs-programming，2 页目标）：修前 13.5pt/71+55%——但快排折行、表被挤成
竖条、标题在页中、右上死洞；修后 **13pt/69+74%**，零折行、表格单行 17pt 通栏、
标题页首、末页通栏大字。连带红利（同参复测）：data-analysis 13pt 填充 91→98+97%；
os-large 2 页 13→**16.5pt**/89+99%（拼装弹性把悬崖推高了 3.5pt）。
prob-band 用 git stash 基线复测**无回归**（裁决两臂 9.5/10.5pt 与基线逐字一致；
50d524c 提交信息里"均衡 13pt 胜 formula 12pt"是另一组参数下的账，非本轮改动所致）。

**诚实边界**：cs 页 1 右下仍留 143mm 空隙——四@8 格在 13.5pt 起 scale<0.75，
被可读性下限锁死不能再伸；这是"密度让位保真"的设计结果，不是缺陷。剩余手段
只剩 AI 精简减量或手写区。

**第二轮（同日）：代码场景的宽度真凶是注释，不是代码**——①的保真代价（基准字号
13.5→13）追根到底：卡宽度的最长行几乎都是长中文注释（`// 快排:分区+递归,...` 一行
60+ 显示列），注释是散文却在享受代码的"禁折行"待遇。两个机制把代价赎了回来：

⑥ **长注释行拆分**（`code-comments.ts`，渲染路径生效、用户源文本不变）：超过
   40 显示列（CJK 记 2）的整行注释拆成多行同前缀注释；行尾长注释挪到代码行上方
   再拆；**代码行一个字符不动**。护栏：字符串里的 `//` 靠引号配平守卫；语言不在
   行注释语法表里原样返回（错拆代码的风险大于收益）；断点标点行尾悬挂（预算 +2）。

⑦ **逐行缩放替代整块缩放**：此前 pre 按最长行整块缩放，一条长行连累全部代码变小
   （code 场景常见 0.76~0.78 ≈ 有效 8.6pt）。现在只缩超宽的那几行（Shiki 每行是
   `.line` span，行级 data-hh-scale 照常汇入块 scale 走选档保护），其余行全尺寸。

数字账（第二轮）：cs **13.5pt 收复**、填充 71+89%（页 0 的"三"只有 typedef 一行
缩 0.94，其余全尺寸；末页三块通栏 14.5~15.5pt）；code-heavy 名义 11→10.5pt 但
此前 11pt 是"整块缩到 0.78 ≈ 8.6pt 有效"，现在绝大多数行全尺寸——名义降半点、
有效字号升约两点；os-large 16.5pt、data-analysis 13pt/98+97% 均不变；
sample 的超高块 + 147% 用 stash 基线复测为**预存条件**（数字逐位一致），非本轮引入。

### 4.8 逐文件调优战役（2026-07-26 起，基准先行，滚动记账）

主目标**排版质量**，按主链路顺序逐文件过；纪律：每站先写诊断脚本定量、单变量实验、
`pnpm bench` 对照（口径见 EXPERIMENT.md「固定基准跑分」）、目检、固化后才进下一站。
**不加新机制、不加新参数**——只在现有机制内调行为/修缺陷（2026-07-26 用户定调）。

**站① chunk-markdown**：诊断先推翻了预设假说——基准套件全部 10 材料的分块粒度健康
（最大块 794 字 < 800，"带标题豁免巨块"路径零触发），切块器对粗粒度材料（cs 只有
6 节）是忠实的，无罪。唯一实锤的可调点：**H1 文档标题被空章头合并规则并进首节**，
cs 的 block-0 因此虚胖成 133mm 巨块。改判：仅 ##+ 空章头并入下一块，H1 标题独立成
迷你"封面卡"（填缝自由度 + 观感自然）。bench 裁决：cs **13.5→14.5pt**、页1 填充
75→81（盒口径）；history-long 页1 +1；代价 prob-band −2pp、network-tables −1pp
（字号页数均不变）。净收益明确，回归锁已加（chunk-markdown.test.ts），基线已更新。

**站② scene-presets（内容统计）**：三口径诊断（现行 A / 剥 URL 的 B / 再剔表格行的 C）
对账 15 材料。裁决：**取 B**——图片/链接 URL 不是正文，危害判例实锤（一张行内
data URI 截图把 ~20 字正文虚增成 4044"字"，cram≥1500 误触发）；全套件零判定翻转
（仅 data-analysis −48 字），bench 与基线完全一致。**C 不取**——表格文本本来就是
要排的内容，计入 charCount 语义正确；网表/表格材料在 C 下字数腰斩（717→202）会让
全部阈值静默重标定，错的是那行"剥掉表格"的注释（已更正，代码从未剥过）。
另记：inlineFormulaCount 的 `$5…$10` 钱符号误报确认存在但不参与任何决策（纯展示），
不修。回归锁已加（rule-engine.test.ts 站②）。

**站③ rule-engine + subject-rules（决策层）**：逻辑逐条过审——硬约束交集、密度
上下界、B1 双跑分叉、S1 诚实记账（nearMiss）均无缺陷，else-if 链恰好总取最严
maxAspect；**阈值数字一个未动**（全部 ◐，校准只认真材料，§4.2 铁律）。两处外围
修缮：① `recommendScene` 零调用孤儿删除；② 学科识别误报实锤——两字别名「能带」
子串命中管理学材料的"不**能带**来满意"，建议了半导体。改判：≤2 字短别名需出现
≥2 次才算证据（真术语必反复出现，双次线不伤召回），长别名一次即可。dump-features
全量复核：仅 word-paste 的误建议消失，合法建议（os/semiconductor/calculus/politics）
全保留。bench 与基线完全一致，回归锁已加（subject-rules.test.ts 站③）。

**站⑤⑥ grid-layout + measure-blocks（联合发现：深压缩救援）**：从 bench 最难看的
network-tables 75% 入手，bySpan 数据揭穿选档缺陷——端口表选 span6=68mm 缩 0.55，
而 span12=58mm 缩 0.98，**更宽的档同时更矮更清晰**，"够得着预设可读线就取最窄"
两头输。第一版在测量层做支配性升档翻车（上下文盲：cram 材料公式块 0.9 也被升宽，
poli-econ/data-analysis 字号各掉 0.5~1pt，已回滚入账）；定稿在**拼装层**挂进
packWithNuggetVariants：缩放 <0.75 的块（与 H3/H4 可读下限同源）按 span 升序试
"高度不增、更清晰"的宽档，**页数不变差才采用**——密排材料升档顶出新页自动被拒。
bench：network-tables 75→80（端口/IP 表恢复全尺寸，目检确认）、poli-econ +1、
os-large 页1 −4pp 换两张表 0.60→0.81/0.50→0.75（16.5pt 下约 9pt→12pt 有效字号，
字号页数均未动）。override 契约升级为 overrides 数组（救援可多块链式）。
回归锁 2 例（grid-search.test.ts 站⑤⑥），基线已更新。

**站⑦⑧ + 支撑文件（收官核查，无改动）**：pack-blocks 有 60 种子随机压测不变量套件
+ 本周五连修战疤，render-layout/render-assets 的原子机制在本轮全部目检中反复验证——
两站核查通过，不为改而改。支撑文件（md-to-html/code-comments 本周新验、
precheck-formulas 纯上报、job-store/pdf-name 琐碎、browser-pool 属性能域、
atom-mask/ai-* 的 prompt 质量没有评测材料不盲调）全部核查通过。

**战役收官账（2026-07-26，七站 + 基准）**：改动 4 处（H1 独立成块/URL 剥离/短别名
双次线/深压缩救援）+ 孤儿清理 2 处；无罪判决 5 项（切块粒度、规则逻辑、adjudicate、
pack、render）。bench 累计：cs 13.5→14.5pt、network-tables 75→80、poli-econ
98→99、真材料 data-analysis 全程 13pt/98+97 未动摇。测试 83→88，每处改动有回归锁。
阈值数字零改动——校准继续等真材料。

**真材料首批入库（2026-07-26 晚，用户提供 ×2）——两份材料各抓出一个真问题**：
① `java-oop.md`（编程课首份真材料，码 31）：「# 章 + ## 节」多 H1 结构立刻暴露
站①封面卡豁免过宽——15 个空章头全被豁免成填缝小卡散落页底，章节关联全断。改判：
**只有全文第一个块的 H1（文档标题）独立**，后续空章头照旧并入下一节（回归锁已加，
bench 套件不受影响）。修后 8pt/2页/95+96%，三列报纸流，代码零折行。
② `db-systems.md`（表格课首份真材料，SQL 码 6）：全部表格是 **Pandoc simple
table**（空格对齐+虚线，Word 转换的典型产物）——引擎只认 GFM 管道表，tableCount=0、
渲染成连排文本。**重大格式缺口**：表格承载知识的课 + Word 转换 = 主流真实形态，
合成材料永远写的是 GFM、永远暴露不了。处置待定（引擎支持 / ⓪ 入口转换 / 手工转），
挂货架待拍板。两份均入 bench 套件（10→12 材料），基线同步。

**真材料观感战役（2026-07-26 深夜，用户反馈"java 字太小、空白太多"）**：盒填充 95%
与观感空白并不矛盾——空白藏在**盒子内部**。定量：java-oop 31 个代码块里 14 个是
单行迷你块（`==`/`super();`），每个在 8pt 下光框内上下 padding 就吃一行半。两个
单变量实验（bench 裁决，均零回归）：
① `pre` 上下 padding 0.6em→0.3em（cram 档本来就是 0.3）：cs +0.5pt/页1 +15pp、
   java +0.5pt、db +0.5pt；
② code 场景块间留白 7.92→4mm（代码框自带背景边界，块间不需要一整格呼吸；
   text-cram 2mm 有先例）：java 再 +0.5pt、db 再 +1pt、code-heavy +0.5pt。
合计 java 8→9pt（+12.5%）/96+95%、db 11→12.5pt/91+99%、cs 15.5pt/95+99%。
剩余选项挂货架：单行迷你代码块内联化（`==` 这类不值得一整个框，需新机制待批）。

### 4.9 机制消融战役（2026-07-28 起，滚动记账）

背景：`GridSearchParams` 累积 13 个可选开关，机制交互空间乘法增长（§4.6/4.7 战役
的遗产）。用户定调：逐开关单变量消融，**无贡献即删**；阈值/取舍必须数据说话。
方法口径：冻结 bench 套件 12 材料逐开关"中和"（关掉或设 ∞），对照 字号/页数/填充/
超高/cramped；数字有疑必目检。

**审计先行（动实验前先查引用）**：`unitsX`、`precision` 两个参数全仓零传参
（产品与测试脚本都只吃默认）——纯管道死面，直接拆除硬编码默认值
（24 格制 / SEARCH_CONFIG.defaultPrecision），bench 与基线逐项一致，零行为变化。
`imageBaseDir` 是实验脚本（run-ab/run-grid/run-scene）在用的正当测试面，保留。
开关面 13 → 11。

**第一轮：backfill / maxAspect（2026-07-28）——两个都判"有贡献，保留"**：

- **backfill=off 消融**：唯一生效判例 data-analysis（S2 弱序 + 用户勾乱序）
  13pt→12pt、填充 98/97→88/88——开关贡献 +1pt/+10pp，实打实。补充对
  poli-econ×politics（弱序第二触发路径）打平无害（材料本就排满，无洞可回填）。
- **maxAspect=∞ 消融**：数字混合（6 判例更差：poli-econ 2页→3页未达标、
  word-paste −2pt;4 判例"更好"：network-tables +1pt/+16pp、os-large/prob-band/
  java-oop 各 +0.5pt）。**目检裁决**：对"收益最大"的 network-tables 双版转 PNG
  对照——∞ 版的 +1pt 靠放行又高又窄的竹竿块换密度，代价是版面秩序崩坏
  （文档大标题沉到页面左下角、章节阅读顺序变 四→一→二→五→三、状态码表被挤成
  一字一行的细柱）。指标收益 = 观感幻觉，约束保住。这是"字号页数指标完全体现
  不出的问题靠目检抓"的又一判例（前例：弃页、标题沉底，EXPERIMENT.md ④）。
- **挂账（调参不是消融，另开一轮）**：os-large/prob-band/java-oop 在放宽 aspect
  下各有 +0.5pt 头寸，提示逐场景 maxAspect 取值（4/1.3/1.5/2）有校准空间——
  需带目检的单变量轮，且警惕 network-tables 式幻觉收益。

下一批候选：repack（默认开）、jointSpan/holeFill（默认开的对偶机制）、
stretchCapPt/stretchLastCapPt（数值挡位）。

**第二轮：repack / jointSpan / holeFill / stretchFill / stretchLastCapPt（2026-07-28）
——五个全判"有贡献，保留"，零反例**：

- **repack=off**：8/12 判例变差、无一变好。最重 cs-programming −2pt（15.5→13.5、
  页1 填充 95→70）、test −1.5pt、os-large −1pt；poli-econ/word-paste/history/db
  各 −0.5pt。页内换位是字号收益最大的单一机制。
- **jointSpan=off**：6/12 变差、无一变好——data-analysis/word-paste 各 −1pt、
  cs/os/history 各 −0.5pt、network-tables 填充 −5pp。
- **holeFill=off**：11/12 一致，唯一生效判例 data-analysis −0.5pt/页1 −5pp——
  单判例但真实，且零成本（其余判例完全不触发）。
- **stretch=off**：字号页数全程不动（符合"只收尾放大"的设计），填充普跌；
  最重 cs 页2 99→55。目检（cs 双版 PNG 对照）：开着是真·满版——末页全宽、
  无截断、结尾干净；关掉后同样内容缩在左 2/3，右侧整条白带，残页观感。
- **lastCap=2**（末页加成归零）：5 判例填充小跌（poli-econ/os-large 页2 各 −2pp、
  java 页2 −3pp）、无一变好。末页更高的伸展顶有效。

**两轮小结**：7 个机制开关全部数据实锤保留，2 个死参数拆除。消融阶段基本收官；
剩余项性质不同：monotonicOrder 需要严格源序（PDF 保真页）判例，冻结套件里没有
——挂账等 knowledge 判例入库后补测；gutterMm/widthTiers/maxAspect 的逐场景取值
与 stretchCapPt 数值挡属**校准**（调值不是留删），另开校准轮并警惕 §4.9 第一轮
记录的 network-tables 式幻觉收益。
