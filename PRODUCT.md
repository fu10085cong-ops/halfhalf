# PRODUCT — 产品定位与增长策略

> 这份文档是非技术的产品/商业判断记录,不影响代码运行。留着是为了不让决策的理由随时间流失
> ——三个月后回头看,"当时为什么决定不做内容社区"比"做了什么"更难重建。

---

## 一句话

半开卷考试小抄生成器——把"一学期知识压进一张纸"从一晚上的排版搏斗变成十分钟。

对外的承诺句是:**你说一页,就是一页——而且是这份内容能用的最大字号。**

这句话的好处是它把最难的技术工作(渲染测量 + 二分搜索 + 网格拼装)翻译成了一个**可验证的保证**,
学生一秒就懂,完全不需要理解原理。下面所有的论证都是它的注脚,不是它的替代品。

---

## 双路径定位与北极星

| | 应急路径(下限,主流量) | 精细路径(上限) |
|---|---|---|
| 用户 | 考前 1~3 天的学生,材料是 Word/课件/聊天记录粘贴 | 愿意维护 Markdown 笔记、逐块打磨的人 |
| 北极星 | **材料 → 可打印 PDF ≤ 10 分钟** | 样张级密度可达 |
| 旅程 | 打开网址 → 对话窗丢材料 → AI 出标准 .md → 一键生成 → 打印 | + AI 精简、学科声明、诊断面板、(远期)网格编辑器 |
| 决策点 | 只留"目标页数"一个 | 全参数暴露 |

入口形态:前端 AI 对话窗(⓪ 结构化入口,架构见 [`DESIGN.md`](./DESIGN.md))——任意内容丢进去,
产出标准 .md,再进排版引擎。**应急路径决定口碑,精细路径决定天花板。**

北极星的计时口径与演练记录见 [`EXPERIMENT.md`](./EXPERIMENT.md) 的「十分钟北极星演练」。

---

## 跟"直接让 AI 总结"的生态位划分

这是最常被问到的问题:**为什么不直接让 AI 生成固定页数的内容?**

### 先说清楚真正的对手

学生的真实经历是:让 AI 总结 → 复制 → 粘进 Word/飞书 → 调字号、分栏、删内容 → 打印预览发现多出
半页 → 再删 → 公式断成两行 → 折腾四十分钟。

注意他**不会把这四十分钟归罪给 AI**,他归罪给"排版好烦"。所以对外说法不该是"我们比 AI 强",而是:

> **你现在怎么让 AI 总结,照旧。总结完复制过来,选页数,打印。**

这个说法不要求用户改变任何现有习惯,只接管最后一步。考前的学生极度风险厌恶,让他换掉整套流程去信任
一个陌生网站门槛太高;在已有结果上多加一步,几乎零风险。

### 为什么"让 AI 直接给一页"是有代价的

有两个办法把内容塞进 N 页:**删内容**,和**调排版**。AI 只有前一个。

实测(poli-econ,6572 字):

| | 每页装得下多少字 |
|---|---|
| 12pt 单栏(AI 输出粘进 Word 的默认样子) | ≈ 600 字 |
| 12pt 单栏 + 手动收窄页边距 | ≈ 730 字 |
| HalfHalf 8pt cram 四栏 | ≈ 3286 字 |

同一张 A4,**排版差异带来 4.5~5.5 倍的容量差**。所以"让 AI 直接生成一页"的真实代价是:它得把这份
笔记删到 600 字左右,**砍掉约 90%**;而走排版这条路,两页装下全文,**删了 0%**。

> **排版是免费的容量,删内容是要付代价的容量。先用哪个,决定了最后损失多少。**
> 正确顺序是:先把排版榨到极限(无损)→ 还是装不下,再让 AI 删(有损)。反过来做,会删掉根本不
> 需要删的那 80%。

### 四道 AI 原理上跨不过去的坎

1. **AI 没有眼睛,做不了排版物理题**:"塞进恰好 2 页、字号最大化"需要真实渲染测量(这条公式多宽、
   这段话在 1/3 页宽的栏里折成几行)——语言模型不渲染就不知道,最多给你 LaTeX 让你自己调,回到排版
   搏斗的原点。HalfHalf 用浏览器实测 + 二分搜索 + 网格拼装,是确定性代码。
2. **刚性原子保真**:AI 重写会动公式和数字(实测判例:AI 统计公式数漏一半)。分工纪律:AI 只碰散文,
   公式/数字/表格由代码保证一字不动(atom-mask 遮罩 + 三道安全网)。考场上一条错公式是灾难。
3. **闭环到纸**:AI 聊天的终点是文本框,HalfHalf 的终点是打印机——密度档、页数命中、可读性钳制全部
   发生在这段最后一公里。
4. **领域判例沉淀**:[`RULES.md`](./RULES.md) 的校准规则(真实小抄照片校准的 cram 档、真材料两侧
   夹逼的公式保护线、学科层顺序刚性)是"半开卷小抄"这个细分领域的 know-how,通用 AI 不会为这么窄的
   场景去校准。

所以关系是**上下游而非竞争**:AI 总结是上游原料(⓪ 对话入口本来就让 AI 当第一棒),HalfHalf 吃掉
它的输出、补上它做不了的下半程。

### 诚实的边界

- 对只想"帮我划个重点"的学生,AI 直接给一页**真的够了**,HalfHalf 对他价值不大。核心用户是
  **"能带多少带多少"的人**——半开卷允许带纸,理性策略就是把信息密度拉满。这两类人不同,别指望一套
  说法通吃。
- 如果 AI 被接上浏览器、真去渲染测量搜索,它**能**做到——但那时候它做的就是这个程序现在做的事。
  区别在于:在聊天框里让它"生成一个两页的文件",它拿不到这些。**这是真实的威胁,不是永久的护城河**,
  所以时间窗有限。

---

## 实测对照数据

材料:`poli-econ.md`(6572 字 · 独立公式 32 · 表 5,用户自有政经笔记)。全部 A4 竖版。

| 路径 | 页数 | 正文字号 |
|---|---|---|
| AI 直出 → 粘进 Word 直接打印(12pt 单栏,Word 默认页边距) | 11 页 | 12pt |
| 同上,手动把页边距改成 10mm | 9 页 | 12pt |
| 手动死磕:单栏,字号一路砍到 6pt 下限 | **5 页 —— 仍塞不进 2 页** | 6pt |
| **超人对照**:引擎替他试 18 轮,找出单栏/多栏全局最优 | 2 页 | 6.5pt |
| **HalfHalf 一键** | **2 页** | **8pt** |

三个结论,一个比一个重要:

1. **11 → 2 页**。最好讲的数字,但也是最弱的论证——对方会想"我自己调调也能压下来"。
2. **他调不下来**。单栏排版字号砍到 6pt(已到可读下限)**依然是 5 页**。学生手里那条"粘进 Word
   慢慢调"的路,在这份材料上不是"慢",是**根本走不通**。
3. **第 4 行是不存在的超人**——18 轮渲染迭代找出 3 栏 6.5pt 的全局最优,真人不可能做到。即便跟它比,
   HalfHalf 仍然大 23% 字号(面积约 1.5 倍)。优势不来自"帮你自动化",来自块级拼装本身比连续分栏流
   更能填满纸。

### 做宣传图必须注意的一点

整页缩略对比会**误导**:cram 的紧行距把 HalfHalf 那页塞得更满,整页看上去反而像"字更小"。
宣传图必须是三联——`11 页缩略 → 2 页缩略 → 局部放大的字号对比`。少了第三张,看图的人得到的印象是
"哦,它把字压小了",正好是反效果。

> ⚠️ 这批数字跑在 2026-07-26 之前的引擎版本上(旧多栏流管线尚在)。数量级结论稳健,精确值需在当前
> 引擎复跑后更新。工程侧的空白/填充率基线归 [`RULES.md`](./RULES.md),不放这里。

---

## 跟"格式工厂"类通用转换工具的区分点

格式工厂是**无损格式转换**(A 转 B,不理解内容语义);HalfHalf 做的是**约束求解**(给定内容和页数
上限,自动求出能让内容合法塞进去的排版参数)。通用转换工具不会、也没有商业动机去理解 Markdown 语义、
保护代码块/公式完整性、自动找最大字号——这不是它们没做,是它们的产品逻辑(广度优先、走量)决定了
不会做。所以不是同一类竞品,不需要在功能列表上正面竞争。

---

## 护城河

二分搜索算法和 Playwright 渲染方案本身不构成壁垒,任何工程师都能照抄。真正难复制的是:

1. **渲染保真度的工程债**——KaTeX 版本冲突、Mermaid 内联样式覆盖、字体跨目录加载失败……这类边界
   bug 只能靠踩坑攒经验,抄代码抄不走踩坑记录(所以 [`CHANGELOG.md`](./CHANGELOG.md) 保留完整经过)。
2. **AI 压缩质量的经验积累**——什么样的精简会破坏可读性、图表该不该转成更紧凑的图表语法,这些判断
   标准是用真实反馈喂出来的,是持续复利的资产。
3. **学生群体内部的口碑/信任**——这类工具天然靠同学之间安利传播,一旦建立"从来没出过错"的口碑,
   新进入者要撬动的是信任而不是功能对比。反过来,一次"AI 把公式改错"级别的翻车会直接透支这个资产。

投入优先级:先把保真度磨扎实,再把 AI 压缩的判断标准喂扎实,最后靠零差错口碑滚雪球。

### 一个尚未解决的内在矛盾

护城河 2 和 4(判例沉淀)都要求**真实材料持续流入**,而 [`DESIGN.md`](./DESIGN.md) 写死了"用户内容
只过内存不落盘、不留存"。两条同时成立的话,上线之后材料来源仍然只有自己和朋友,判例库的增速跟今天
完全一样。

要么现在解决(比如导出时一个显式勾选的「愿意匿名提供这份材料帮助改进排版」),要么承认判例库靠人肉
喂养。**上线那一刻没做,之后就错过了最肥的收集期。** 尚未决策。

---

## 学生内容社区(想法阶段,尚未开发)

**设想**:允许用户分享整理好的学科知识点总结,订阅制解锁他人分享的内容,创作者分成。

**现状判断**:需求信号是真实的(小红书上已经有大量同类"知识点总结"帖子在自然传播,创作者靠署名/
流量变现,不是靠直接收费),但直接开发付费市场级功能风险和成本都过高,暂不排期:

- **学术诚信边界**必须卡死在"自己整理的知识点总结",明确排除"历年真题/答案/未公开课件原文"——前者
  风险接近教辅资料,后者是实际的红线,一旦出现会反噬上面说的口碑护城河,且有平台/校方层面的封禁风险。
- **工程规模**跟现有系统完全不是一个量级——现在没有账号体系、没有面向用户内容的持久化存储、没有支付
  基础设施,做这套东西的工程量可能超过整个排版引擎,且是完全不同的技能栈(支付合规、内容审核、社区
  运营)。

**低成本验证路径**:

1. 不做账号/支付/审核系统,先做一个极简的"导出署名水印"选项,复用现有排版引擎——验证创作者是否认可
   HalfHalf 的排版质量,愿不愿意用它处理本来就要发布的内容。
2. 内容冷启动不找陌生创作者,通过朋友网络覆盖不同专业,在小红书自然发布 + 软性带上产品链接,同时完成
   内容启动和产品推广。开始前对齐一句话的内容准则("总结自己的理解,不要照抄课件/真题"),避免边界问题。
3. 朋友产出内容不是长期供给策略,只是启动种子;小红书对硬广限流敏感,避免统一模板/矩阵号观感,保持
   各自的个人风格。
4. 验证通过(创作者认可排版质量、内容能自然传播)之后,再考虑要不要往正式的订阅/分成方向投入工程。

---

## 冷启动:前 10 个用户的价值不是用户数

是**演练日志**。[`EXPERIMENT.md`](./EXPERIMENT.md) 里那张 T1~T5 的表现在是空的,填满它需要的不是
流量,是 10 次面对面的观察——看他们卡在哪一步。

时间窗:半开卷是考试驱动的尖峰需求。暑假期间没有真实用户可验证,这几个月应该用来把 ⓪ 与部署打通,
开学后抓一批真人试用,把期中考当第一次实弹演习。**在没人用的时候把引擎从 90 分磨到 99 分,和磨到
90 分,考试那天的差别可能是零。**
